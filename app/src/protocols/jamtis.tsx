// protocols/jamtis.tsx — STUB. Replaced wholesale by the v6.0.9 simulator build.
// If this text survives to review, the JamtisView simulator did NOT land.
import * as React from "react";
import type { MoneroLive } from "@/data/types";

interface ViewProps {
  data: MoneroLive;
  bg?: { intensity?: "calm" | "busy" | "chaotic"; scan?: boolean };
}

export function JamtisView(_props: ViewProps) {
  return null;
}
