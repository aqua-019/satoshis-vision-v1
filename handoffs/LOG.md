# Loop log — one line per completed task: task_id · outcome · PR

- XMRIRISH-20260731-01 · done — v6.1.0 deleted the dead v4 front-end (100 files, ~82.5k lines); verify-origins rewritten app-only; sitemap generated from app/scripts/routes.mjs; CLAUDE.md project description rewritten to standing facts only, no dated run log, L3 sections byte-identical · https://github.com/aqua-019/satoshis-vision-v1/pull/146
- XMRIRISH-20260801-02 · done — v6.1.1 Future tab: repo pulse goes 2→4 repos and splits push age from issue age (src=ghrepo gains issueAt from a concurrent second GitHub call, browser cost unchanged at 1 request/repo); all paid-API framing removed from app/src; the unconfirmed MoneroSpace lineage claim removed from 4 rendered sites and gated against; registry's stale "2.5s poll" corrected to the v6.0.6 tiers; verify-future.mjs de-networkidled, hardened and wired into verify:e2e (CI 24→25 distinct gates) · https://github.com/aqua-019/satoshis-vision-v1/pull/147
- XMRIRISH-20260801-03 · done — v6.1.2 theme system: `@layer reset, base, theme, components, utilities` declared once with every rule layered; 18 `@property`-registered colour roles in `@layer base` with one-way aliases (`--tk-accent: var(--accent-data)`) and a theme-invariant hue scale; classic is the default and phosphor joins classic/indigo; per-theme aurora; one `ThemeToggle` on Main Home and in `⌘ DESIGN`. Colour audit: mempool/ 129→0, pages/→0, plus the 14 longhand `#ff7a1a` sites in protocols/ (diagram palettes otherwise untouched — a theme must not change what a diagram means). Three defects no gate covered: layer order silently killed all 49 indigo chrome overrides (`theme` sits before `components`); `verify-shots.mjs` counted uncompared shots toward a pixel-identical claim, only in the green case; `--out` without a trailing slash wrote to a sibling path. Phosphor structural moved `#39ff88`→`#33ff33` on a measured ΔE00 7.4 collision with the protocols diagram green; the predicted amber collision measured 12.5 and was left alone. `verify-contrast` + `verify-ground` de-networkidled and wired into `verify:e2e` (10→12) · https://github.com/aqua-019/satoshis-vision-v1/pull/148
- XMRIRISH-20260801-04 · done — v6.1.3 motion & transition foundation, completing the scope PR #149 merged with half-unmet. Four duration + five easing tokens declared once in `@layer base` and zeroed as a set under reduce; every remaining literal carries a `// D0651:` reason. D0673 transform-only bar fills + `verify-gpu`. Hand-rolled feature-detected View Transitions with the fallback treated as the MAIN path (Tor = Firefox ESR; FF shipped VT in 144), `React.lazy` kept and only observed via a resolved-key `Set`. Navigation semantics: three-target scroll restoration (`window.scrollTo(0,0)` was a no-op on every desktop — `main.main` is the scroller), focus handoff to a labelled `<main>`, `.sr-only` h1s on the four pages that rendered none, a persistent announcer, a skip link, and `useUrlState` fixing two sibling-param drops. 27-surface reduced-motion audit as the deliverable — 19 already compliant · 3 fixed · 5 not applicable, plus six motion defects found and closed, one of them (`terminal`'s setTimeout typewriter) invisible to the runtime animation census that scored it clean. `/simulate` had been shipping an empty `#root` on one route (one space of regex slack duplicated in two files). Eight new gates, all break-tested; `verify-prng` §6 widened from `src/design/` to all of `src/` after an adversarial injection into `src/routes/` passed the whole static chain green. Recorded honestly rather than resolved: `/monero/markets` @1440 differs reproducibly and sub-perceptually (every delta exactly ±1 channel on background dither) with **cause unproven**, and `verify-shots.mjs` is structurally blind to CSS animation (`freezeAmbient`) and order-dependent with a ~17-shot noise floor — both now written into that gate's own header · https://github.com/aqua-019/satoshis-vision-v1/pull/150
- XMRIRISH-20260801-05 · done — v6.1.4 PR A (PARTIAL, split at the mechanical/editorial seam): `MoneroLive`'s four status booleans (`live`/`ready`/`marketReady`/`stale` — sixteen states, most nonsense, and unable to express "never succeeded AND now failing" or "the market is down") replaced by one per-endpoint discriminated union in new `data/feed-status.ts`, derived during render (D1624) rather than stored at commit time from refs that had already moved on; 92 reads migrated across 13 files (~40 edits, five files aliased once); `useRepoPulse` now returns `{pulse, at, state}` like its three siblings instead of discarding the state machine, so a dead `/api/feeds` proxy names the endpoint on all nine pulse surfaces instead of spinning forever. Behaviour-preserving on purpose: `feedDegraded` keeps the tiers' pair-AND rule, `lastUpdate` stays the per-commit heartbeat, and `error` still renders the `loading` copy — all three named in the PR body as PR B's, alongside the 15 `.stale` sites and a found mis-attribution (a COINGECKO badge flagged stale by the node cascade dying). Two new gates (`verify-feedstatus`, `verify-effects`) plus two orphans wired in (`verify-allreal-dom`, `verify-tiers-dom` — both pass unmodified on the refactored tree, having been written against the old booleans); CI 35 → 39 distinct. Also found: `.claude/hooks/stop-gate.sh` has never existed, which explains prompt 03 shipping with every DONE-CRITERIA box unticked · https://github.com/aqua-019/satoshis-vision-v1/pull/151
- XMRIRISH-20260802-06 · done (PARTIAL) — v6.1.4 PR B items 0–4, opened as a PARTIAL at the operator's request after the scope shortfall was surfaced BEFORE opening rather than after. PR A made the bad states unrepresentable and changed no pixels; this closes the gap that left. A cold load with every node down said CONNECTING forever on all FIVE chrome surfaces (the plan said four; tsc found HomePage) — now NO NODE RESPONSE, endpoint named in the tooltip, offline outranking it, wording measured at 390px for reflow. The seven /network charts dimmed on the pair-AND global where the specific key was in scope; each now dims on its own endpoint, plus three same-file mis-attributions (Mempool KPI gated on network, Fork KPI gated on nothing, mempoolSeries blanked by network). HomePage's COINGECKO badge stopped blaming the node cascade. All ten lastUpdate consumers moved to per-endpoint time, so clocks freeze at last-good in an outage instead of ticking over unrefreshed numbers — verified 01:29:41 → :41. Two new gates: verify-cls (Chromium-pinned, re-measures every CI run and prints the number; thresholds from 8 measurement runs on the unmodified tree) and verify-failure (drives ONE endpoint to a 500 while siblings answer — the repo's first such test). makeReporter gained skip()/fixture() counters, which I had twice asserted already existed; fixture() still has zero callers. The gates found three things I did not: a constant tip fixture makes a /blocks outage invisible, two panels had no staleness treatment at all, and a CLS of 0.0841 for the documented unreserved-fallback regression sits BELOW the Web Vitals 0.1 bound — disproving the loose-threshold option empirically. Instruction figure corrected: 18 blind fresh="live" badges, not 26. Item 5 (10 pieces, itemised in the PR body and handoff §3) deferred; the ProvFreshness guard hazard written into handoff §8 because item 5 starts with no context · https://github.com/aqua-019/satoshis-vision-v1/pull/152
- XMRIRISH-20260802-07 · done — v6.1.4 PR C, item 5's first three sub-items, not a partial. 18 badges pulsed a hardcoded `fresh="live"` through a total outage, two of them on /network four words from the literal `· stale` their own header rendered. `ProvFreshness` gains `error` and its FIRST exhaustiveness guard — the suffixes were two independent ternaries, proven by a pre-PR sweep that widened the union on merged main and got nine errors tree-wide and ZERO in provenance.tsx. `<NodeProvenance keys={…} status={…}>` derives freshness per-endpoint (never `feedDegraded`, worst-of, `assertNever`), with props `Omit<…,"fresh">` and a non-empty tuple so `keys={[]}` and `fresh="live"` are both type errors. New `verify-provenance.mjs` (38 assertions, into `verify:static`) bans every literal outside a reasoned allowlist checked in BOTH directions. Guard sweep: 9 sites on main → 12 here; the handoff's seven all confirmed at their named lines, **plus two nobody had counted** (`useOnline.ts:94` Record, `:109` return-type — both invisible to a grep for assertNever/satisfies), and `Footer:25` does NOT borrow. Counts corrected: 47 call sites not 48; `FeedPhase` has four members so the sweep adds a FIFTH; and `MarketsPage:263` — listed as one of the two "correct" ternaries — gated on `hasData`, true for phase `stale`, so it showed a live dot on a feed confirmed down. Also found: `pulse` was a second live-dot backdoor with zero call sites; `SourceBadge`'s `detail={prefix ?? f.detail}` silently ate the failure signal at three call sites; `useMarketHistory` parsed its cache's `at` then discarded it, so /markets had no arrival time at all. Per-panel stamps on 29 of 32 live panels via `oldestFreshAt` (3 named exceptions, all static); `.prov-fresh--stale` into the reduce kill-list — it invoked the `blink` keyframe but carried no `.blink` class. `verify-failure` 8→16, now proving the freeze BOTH ways after a one-sided version passed while six healthy panels correctly advanced. 7 break tests incl. one ellipsis failing all 11 routes with "still suspended". CLS 0.0009/0.0000/0.0001/0.0001 — the gate had never measured an honest badge. `MarketsPage:291`'s COINGECKO∣SESSION mis-attribution on a node-derived value recorded with both readings and its DataLegend knock-on, deliberately unfixed · https://github.com/aqua-019/satoshis-vision-v1/pull/153
- XMRIRISH-20260802-08 · done — housekeeping, not one of the nineteen: the three v6 design mockups (`nav-ia-mockup`, `markets-network-mockup`, `coldboot-splash`) committed byte-for-byte under `docs/v6-mockups/` with an index README, so the five prompts that build against them read a path instead of an attachment. Each attachment matched to its row by `<title>` rather than by its hyphen-stripped download stem. Docs only, five files added and zero modified; build 11 routes, `verify:static` 16, `verify:e2e` 22, CI 42 distinct — all unchanged and green. One gate does walk the new directory and is reported rather than excluded: `verify-future.mjs:99` walks the repo root for `.md`/`.html` and its `SKIP_DIRS` never excluded `docs/`, so its scope is unchanged (13 `docs/*.md` already pass it daily) and all four new files test clean against gate 15 · https://github.com/aqua-019/satoshis-vision-v1/pull/154

- XMRIRISH-20260802-09 · done — v6.1.4 PR D, the last of prompt 05: the UI a failing feed needs. Structure-aware skeletons on four surfaces via one `<Swap>` primitive that mounts content at opacity 0 and overlays the skeleton (replacing it would strand `useChartMetrics`' once-only ref measurement forever); `PanelBoundary` — the app had exactly one error boundary, at the root — naming its endpoint BY TYPE from `FEED_ENDPOINT` with an inline retry that really refetches; a degraded banner anchored to `.shell`, whose top edge IS the topbar's bottom edge by construction, so it needs no offset constant across the topbar's three heights; offline outranking it; jitter from a golden-ratio Weyl sequence, low-discrepancy and exhaustively gated rather than `Math.random()`; and `/api/status` reporting CONFIGURATION only, because each Vercel function has its own module registry so `_healthSnapshot()` there is structurally, permanently empty — not a cold-start transient. `fixture()` got its first callers repo-wide, paired with a `fetch` counter proving zero probes. Three new gates, 42 -> 45 reached by CI. Found: ten of 22 e2e gates call `.route()` zero times and were measuring a fully degraded feed silently, because `serve-dist` answered `/api/*` with 200 text/html — fixed to 501 JSON, which makes every gate honest at once. The new gates caught two of my own bugs: a single-slot snapshot cache that thrashed into React #185 and blanked whole routes, and a 4px reservation derived from the populated state. CLS 0.0002/0.0000/0.0000/0.0000 · https://github.com/aqua-019/satoshis-vision-v1/pull/155

- XMRIRISH-20260802-10 · done — v6.1.5 PR A, the ruler: the first gates in this repo that measure COST rather than correctness. `verify-bundle.mjs` budgets eager JS, CSS, per-route first-load closure, total JS and Vite's own 500kB backstop, fed by a `bundleGraph()` Vite plugin implementing only `writeBundle` — a hook that structurally cannot alter output, checked rather than trusted by diffing dist/ built with and without it (71 of 71 assets byte-identical). `verify-vitals.mjs` is the first COMMITTED LCP harness here: PERF-BASELINE.md described one and tabulated numbers, but tree-wide hits for `largest-contentful-paint`, `longtask` and `interactionId` were zero, and v6.0.9's prerendering means those figures measured a different critical path entirely. Zero new dependencies; package-lock byte-identical. Gates 45 → 47 reached by CI. The mocked CLS pass — added because `verify-cls` had called `ctx.route()` zero times, so every CLS number ever recorded here described a page where data never arrives — immediately found a real production defect: `/` reads 0.3482 healthy against 0.0006 degraded, stable across 8 runs, because `DIV.ticker-strip` grows h25→h38 when the first market tick lands and displaces `.shell` by 3px. Recorded with a defect ceiling, deliberately NOT fixed, because fixing it would destroy the equality that makes this PR's baseline mean anything. The 14 gates CI does not reach were each RUN: 4 fail, and 3 of the 10 that exit 0 measure nothing — `verify-memperf` prints "every canvas view holds ≥30fps" while all six report `n/a`, and `verify-perf-classic` exits 0 down its INCONCLUSIVE path, which is exactly why the new gate reports a counted `skip` instead. Both budgets are proven red in the PR's own CI history with numbers, not claimed: run #63 `Gate: bundle budgets` FAILURE (eager 127.6 KB gzip / 32 chunks vs 78.05 / 35), run #66 `verify-cls` 8 passed / 2 failed (`/mempool` 0.0841 degraded, 0.1102 healthy) — CI reproducing the sandbox figures to four decimals. The break test produced three findings of its own: `git checkout -- <file>` reads the INDEX and silently no-ops on a committed break (v6.1.3's failure recurring inside the PR adding the gate meant to catch it); a failing `build` job skips every hardening gate via `needs: build`, so the two proofs must be separate commits; and `upload-artifact@v4` skips hidden files, so `.perf/` uploaded nothing while the step stayed green. Also corrected: CLAUDE.md's pre-existing 9/16/22 gate counts and its "api/ is CommonJS" (it is mixed), and the 0.07/0.0841/0.0853 unreserved-fallback figures reconciled to one place. · PR link below

- XMRIRISH-20260802-11 · done — v6.1.5 PR B, the optimisation the ruler was built to measure. `/` healthy-pass CLS **0.3483 → 0.0012**, fixed at its cause. PR A's diagnosis (ticker h25→h38 displacing `.shell` 3px) was disproved three ways before a line was edited: arithmetic (3/844 caps one entry at 0.0036 even at impact 1.0 — 0.3482 needs ~294px), polarity (the degraded pass renders the LONGEST label and reads 580× better), layout (`styles.css:2347` hides every `.tk` at 390px, so no price element remains to grow). `verify-cls` now captures `entry.sources` PER ENTRY — CLS is a sum, and one ~294px shift and ~98 3px ones need opposite fixes — which named the real mover: `.navtop-toggle` travels **Δx−314** when `.topbar`'s `flex-wrap` re-solves, because `.ticker-strip`'s `margin-left:auto` eats the space freed when CONNECTING (202px) becomes LIVE (159px) and evicts the hamburger to a second row. Both of PR A's observations were CONFIRMED in the same entry; only the attribution of the score was wrong. Prediction and instrument met: 314 × 0.93 = 292 against a required ~294. Four candidates ranked by stylesheet injection before editing — `min-height` reserve (what the superseded diagnosis implied) **0.0304, 6× the ceiling and it would have looked like a fix**; `flex-wrap:nowrap` 0.1018–0.1020 plus an 824px vertical shift; pinning the pill's `min-width` **0.0008**. 29ch set by measuring all five rendered labels (LIVE 67 · OFFLINE 88 · CONNECTING 110 · NO NODE RESPONSE 153 · STALE·reconnecting 181 at 1ch=6.3125px) — the advisory 21ch was 132.56px and would have left the two longest still shifting, a number taken from a count instead of a measurement inside a fix for a defect caused by exactly that. Item 6: 21 simulators made lazy via `views/index.tsx:65`'s EXISTING `lazyView`, `/simulate` first load **133,680 → 84,934 B gzip (−36.5%)**, `SimulatePage` dethroned as largest chunk — costs stated, not netted: total JS +6,439 B, chunks 35 → 53. `verify-reduce` §1 would have gone green and VACUOUS (it waits `main`+1500ms, which now resolves on a Suspense fallback for all 21 surfaces; §2's two-URL control could not have caught it) so a `.art.proto` mount assertion lands in the same commit, break-tested red on all 21. Unplanned defect found by chasing a SKIP that printed a number anyway: the mobile nav drawer (`z-index:200`, x86–390) painted over its own ✕ at 97% opacity — Escape saved pointer users but the rule is `≤768px`, where there is no Escape key and no backdrop, so a touch user had no way to close it; two wrong diagnoses were written and probe-disproved before the right one, one of them already committed into a comment and corrected in place. Items 7+8 built as specified and **deliberately NOT shipped**: no blocking benefit on either route (medians overlap, trend worse) and a reproducible −1235px scroll-height lie on /education, with a first single-run pass (+278/−74ms) shown to be noise that 3 runs sign-reversed. CLS fix 2 a measured no-op with the null proven non-vacuous (all three fallbacks captured rendering, `main` 0..4786px). Item 5 INP 96/152/184ms vs 400. Item 9 already correct — one scroll listener, already passive. `verify-bundle`'s `10 tab modules` corrected to 9 · https://github.com/aqua-019/satoshis-vision-v1/pull/157

- XMRIRISH-20260802-12 · done — v6.1.6, the nav restructure: 11 flat top-level items to 6 sections (Live · Monero · Learn · Future · Operate · About) plus the ⌘K palette that makes 6 viable, and the gate sweep a URL rename turns out to require. `scripts/routes.mjs` became the single authority — `R` (13 named constants), `ROUTES`, `REDIRECTS` (12) and `HASH_REDIRECTS` (2) — with `App.tsx` MAPPING over REDIRECTS rather than restating it, and `NavTop`/`RootBoundary`/`index.html`/`useViewTransitionNavigate`/`ia.ts` all deriving from `R`; the "four route lists, one truth" TODO closes except `verify-lib.mjs`'s 43-entry TEST SURFACE, which is deliberately not a route list. `vercel.json` restates the 12 redirects because JSON cannot import, so `verify-redirects.mjs` makes drift a build failure: **12 matched / 0 drift**, printed, never a bare pass. `/pro` DROPPED after being reinstated by a stale relay and reverted — it never existed as a live route, and §4 guards its absence in both directions so a later "free insurance" argument fails the build. Redirect tally reported as **12 server · 12 client · 2 hash · 3 identity**, never as "15/15", because a fragment is never transmitted to a server. **Three defects the gates caught in my own work.** `verify-ia` §7 required every route to be an IA leaf, `/` cannot be one, and the exemption sat in the PROSE and not the CODE — so the only passing shape was a Home leaf inside a section, and since a section navigates to `cols[0].items[0].p`, **clicking "Live" went to Home**; a gate whose prose and code disagree does not fail to catch a bug, it manufactures one. `.nav-noscript` (added because 6 of 13 routes had NO anchor with the bundle dead — six pages prerendered to real HTML that nothing could reach) shipped visible and was hidden on boot, costing **0.1778 CLS**, attributed by the gate as Δy−161 "node removed"; inverted to default-hidden, `/` degraded **0.1778 → 0.0002**. And `verify-cls`'s five stale budget keys all read the SAME ~0.177 in both passes — identical numbers across different routes being the signature of five keys measuring one shared home→redirect transition, since `ROUTES = Object.keys(MEASURED)` means a key can never miss its own lookup; repointed via `R`, every one now **0.0000**. Measuring BEFORE repointing is what separated those two. Sweep scoped to CI-reached + npm-wired: 22 e2e gates, ~96 actionable literals, with `verify-ia`/`verify-redirects` excluded by design (their old paths are the redirect SOURCES) and bannered DO NOT SWEEP. Three blind spots the sweep pattern could not see: a REGEX literal (`waitForURL(/\/simulate/)`, killed the chain for 30s), dead SELECTORS (`nav.topnav` × 7 in `verify-motion` — I told the agent not to touch selectors), and a one-shot rAF tripping D0699. `SIM_ROUTES` was silently `[]` — not a vacuous pass but a false FAILURE plus a silenced NOISE FLOOR caveat — and the first fix over-matched `/learn/simulators`, the same missing-boundary bug in the commit fixing the other one. **68 stale literals in orphaned gates left knowingly**, per file, in Known Issues: nothing runs them, so a fix cannot be proven correct. `.claude/hooks/session-start.sh` printed "0\n0 static routes" because `grep -c` returned 0 AND exited 1 — a prompt premise ("its route count is derived, so it survives") that I also checked early and wrongly confirmed. Palette prints its counting rule — `sections 6 · leaves 63 · home 1 · actions 7 = 77` — so "≥70" is falsifiable rather than an assertion about an unstated denominator. Requirement 9 built and measured (80ms crossing → 0 fetches, 250ms → 1, dedup holds), with the ~200-300ms real-world win reported UNMEASURABLE here: `serve-dist` sets no Cache-Control, so even a replay cannot show a cache hit. Gates on disk 63 → 66 · `verify:static` 17 → 19 · `verify:e2e` 24 → 25 · **CI 47 → 50 distinct** · routes 11 → 13 · Monero tabs 9 → 7. cssGz 14,877 → 15,882 · eagerJsGz 79,947 → 85,028, both inside budget. Open and NOT fixed: 19 sub-12px declarations in `styles-legibility.css` are an 11px-vs-12px STANDARDS conflict (v6.0.10 set 11 deliberately; `verify-legibility` asserts no rendered floor on any CSS selector at all), and query-string survival through Vercel's 301 is undocumented and unverifiable without a preview deploy — `/simulate/:id → /learn/sim?p=:id` depends on it and the client mirror cannot compensate, because the 301 fires before the SPA loads. Then `director-quality` ran for real and returned FINDINGS, not CLEAR — ten, in gates this same PR added and I had personally cleared. Two could not go red under ANY tree: `R.ok(X || true, …)`, and a `catch` turning a whole section into `R.skip`, which break-tested to `21 passed · 0 failed`, exit 0, GREEN, with `npm run build` also passing. Both had survived a break test, because break-testing a FILE proves the file can go red and says nothing about any individual assertion in it — which is how all 396 `R.ok` calls in this repo have ever been validated. An ELEVENTH was mine, found by checking the COMMITTED tree to confirm the second was really gone rather than trusting the edit I remembered — the identical mistake sat three lines away in the same file, `if (!Array.isArray(iaModule.IA)) R.skip(…)` with every §7 assertion in that branch's `else`, and would have read `21 passed · 1 skipped · 0 failed`, exit 0. The class RECURS LOCALLY, because reaching for `skip` where `ok(false)` belongs is a habit of mind about what `skip` means rather than a typo, so a review handing you N findings is telling you where to LOOK and never how many there are. Ten of the eleven CORRECTED here, each break-tested on its own (16 new or changed assertions, 16 individual tests, one of which had to be re-planted after the first version reddened through the WRONG code path — a break test that goes red for the wrong reason is indistinguishable from one that worked unless you read the failure text instead of the exit code): the two dead assertions, a `hasChild` clause letting a route count as present when only a descendant was, a 220ms hover timer asserted with a measured 6.8ms margin on a shared runner, a rAF exemption keyed on FILE PATH that blanket-exempted the always-mounted nav shell (now a per-call-site `D0699-EXEMPT` marker, 12 across 9 files, proven with a planted infinite loop that used to pass clean), `RedirectTo` having zero runtime coverage (now 9 browser assertions), a 404 sweep whose only discriminator was one copy string (removing it left "all 64 destinations resolve" passing vacuously), `verify-vitals` keys as string literals with a branch chain that had no else, and two `DO NOT SWEEP` banners protecting nothing. The remaining item is DEFERRED: a static vacuity screen over the whole suite, held off this PR on `claude/verify-vacuity-screen` because it is gate-tooling and carries the same flag that just caught the first two. CI then executed `verify-vitals` for the first time ever and CLOSED the budget question in the opposite direction from the worry — every assertion would have passed, `/` LCP 1804 inside its original 1788-1852 band — so no ceiling moved and the recorded bimodality flag is WITHDRAWN (CI spreads 24/20/16/12ms, under 1.3%). What CI did expose: the contention guard is ANTI-CORRELATED with contention, clearing the sandbox run that produced 3860ms LCPs and skipping all four good CI routes at 1.62-1.64x, because `CPU_REF_MS` measures core speed, not contention · https://github.com/aqua-019/satoshis-vision-v1/pull/159

- XMRIRISH-20260803-13 · done — AQUA stack v4 → v4.1, exact-anchor patch: the lead contract the prompt-07 pilot earned. Four rules that existed only as pilot narrative become enforceable stack text — a verification artifact is `DONE` only with a **two-polarity execution transcript per new or modified assertion** (untouched ones grandfathered), because break-testing a FILE proves the file can go red and says nothing about any assertion in it, which is how all 396 `R.ok` calls in this repo had ever been validated and how `R.ok(X || true, …)` survived; every claim in every return is **executed / read (cited) / UNVERIFIED**, reviewers included, since an APPROVE is a return like any other; directors self-ID spawn mode as a mandatory FIRST output proven by tool attempt, so a director that was never created is visible as an absence; and PREFLIGHT widens to mechanical-rule dispatches whose reply carries `NOT-MATCHED:`, with written assumption disposition (ACCEPTED / CORRECTED / DEFERRED → §8). Block prose synced: three rules not two, gate tooling added to the mandatory re-judgment list, cloud-session reality notes (`gh` absent, Stop hook machine-scope). Mechanical application only — 17 `sub1()` operations, 44 assertions, nothing authored. All 44 anchors verified present exactly once and the whole patch dry-run in memory BEFORE any write; 13 files, +57/−24. Fail state recorded first (three prompt zeros plus three more, so every pass row has a measured fail-side counterpart rather than three of ten), then all 12 pass rows. Head byte-identical at 46,343 chars, verified independently of the script's own hash assertion; 6 hunks, lowest line 638 against a block header at 636. **The patch's own first rule caught its author before merge**: the plan claimed the stack-block sha256 `06F990F7…933DC` proved the agent files carried the corrected source's four em-dashes — it proves nothing of the kind, since the em-dashes live only in `.claude/agents/*.md` and both prompt variants produce a byte-identical CLAUDE.md tail (the operator ran both: same hash), and every other row in the prompt's own table also passes on the corrupted variant. A read claim carrying an executed claim's weight, which is the exact species the three-kinds rule adds provenance for. Two decisive rows added as the fix — `cat .claude/agents/*.md | sha256sum` → `f641086f…f13c` (corrupted variant `d2a7fe0b…`) and an em-dash canary — both green, with the extraction source checked before any write. Also corrected: a bare "50-gate suite" figure, re-derived as executed (50 distinct at 2c30b94, after a first count of 49 that missed `npm run verify:bundle` indirection; the 47 on record was right for 0e7f73b, raised 47 → 50 by PR #159). Config only — no application code, `.claude/hooks/` and `.claude/settings.json` untouched · PR link below

- XMRIRISH-20260803-14 · done — v6.1.7, the public node population panel: `/live/network`'s peer panel has been a paused placeholder since v5 and **cannot** be filled (the public cascade runs restricted RPC, so `get_connections`/`get_peer_list` return *Method not found* and every peer counter in `get_info` is a literal `0`, which `api/xmr.js:922-928` refuses to publish). monero.fail is a different dataset and an honest one, so the panel says "public node population, sampled by monero.fail" and never implies it is this site's peers. New CommonJS `/api/nodes` proxy (CSP is `connect-src 'self'`, so the browser reaches no third party), a fifth `NETWORK` `ProvSource` distinct from `NODE`, a `.col-2` two-part shell so the v7 "your node" half slots in without a redesign, and the orphaned `api/monero.js` settled by **deletion** — file and its `vercel.json` `functions` key in the same commit, because a key naming an absent file is a hard Vercel build error that fails at deploy rather than at any gate. **Failure is HTTP 200 `status:"unavailable"`, not 502**: `http.ts:13` collapses every non-2xx to `null`, so a 502 arrives as an indistinguishable nothing and the panel's two failure sentences ("/api/nodes did not answer" vs "/api/nodes answered and reports monero.fail did not") could not differ. **Hosts never reach the wire** — the `Node` type has no `host` field, so the leak assertion is true by construction, and the fail-side polarity was still produced by threading one back in and watching it red. Impossible-state envelope (`counts`/`availability`/`height` are `null`, never zeros) makes "render 0 reachable nodes" a compile error. **Eleven gate defects surviving into lead review, and the class is one class**: code no input can distinguish, sitting behind a comment rationalising it. `excluded: 0` was a hardcoded constant dressed as a census field; the four failure arms of `api/nodes.js` were **literally identical** (`if (AbortError) reason='upstream-timeout' else reason='upstream-timeout'`), so §5's timeout assertion was green on every throwing input while the envelope fabricated a cause on a live surface — now four reasons with four producible states; the same block also captured `upstreamStatus` and never read it, carried an unreachable `||` default, and served the `unavailable` exit as `no-store`, opting out of CDN collapse **exactly during an upstream outage** and turning every visitor into a fresh fetch against a third party already struggling (45/45 like its siblings now). Five of seven `verify-nodes` loops were unguarded — the operator emptied `vercel.json`'s `functions` to `{}` and the parity group stayed green, because a loop over nothing passes. `verify-cls` never mocked `/api/nodes`, so **both** CLS numbers measured the empty state; the fix invokes the real handler rather than reconstructing the envelope (my first mock said `seen: 25` where the handler says `24`), plus a standing assertion that the mock reaches the panel. `verify-nodes-dom`'s 12px rule selected `[class*="grid"]` and matched **zero elements** — found by the design-reviewer, missed by the gate author and by me; the guard is the deliverable, proven by restoring the broken selector on a throwaway copy where the measure assertion still passed and only the guard failed. The phantom parity assertion `verify-lib.mjs:222-224` advertised **did not exist**; made real via a `verify-fixtures.mjs` split (playwright is verify-lib's first import and the offline job cannot load it), asserting `path`+`file`+**`kind`** — the field this PR extends with a value not previously in the set — as two separate assertions so a cosmetic reorder reds with "order differs" rather than inviting someone to loosen parity. **The sharpest defect was found by CI, not by me**: `verify-nodes` group 2 called the handler with no fetch stub, making a **real HTTPS request to monero.fail**, and hung the job for eight minutes beside neighbours finishing in one second — my sandbox hid it by failing fast with 403 on CONNECT, i.e. the stricter environment concealed the bug. Fixed with a throwing sentinel plus an `UNSTUBBED_FETCH_CALLS` counter; the break test shows `GET → 200` **still passing** with the defect present, because the handler's own catch absorbs the throw. Rule that generalises: **any assertion whose subject is a selected set needs a companion asserting the set is non-empty.** Six break-test mutations stranded across two sessions by five distinct mechanisms (`cd` drift ×2, `git checkout --` silently no-oping on an **untracked** file, a killed wrapper, an interrupted call, and one mutation that never applied) — so the method changed: **probe a state the code already produces instead of mutating source**, and verify a revert by `git status`, never by the line you printed saying you reverted. `pkill -f serve-dist` matches its own shell and killed the caller in both sessions. Blocked and named as blocked: the live node-count comparison (sandbox egress 403), carried as `R.skip` with the preview command, counted in its own column. Deferred with reasoning: the D0837 sparkline grid, whose row count is payload-dependent on the route with the tightest CLS ceiling in the repo and the least Verify coverage. Reported not resolved: the 11px/12px floor conflict, and `--c-50`/SESSION's missing contrast baseline (the new `--b-50` `#5b9dff` measures **7.31:1**, above `--p-50`'s shipping 6.89:1, and joins neither table by the same precedent). Counts 67 → 70 files · 64 → **66 gates** · 2 → **3 shared modules** named by filename in all three sentences · orphans unchanged at **11** · `verify:e2e` 25 → 26 · https://github.com/aqua-019/satoshis-vision-v1/pull/161

- XMRIRISH-20260803-15 · done — v6.1.8, the cold boot: a full-bleed seeded decrypt that resolves into a real Monero transaction and closes on `signer_index ??? — NOT ENCODED IN THE PROTOCOL`, a HUD console that **gates** on the user rather than timing out, and a rewritten Main Home the console expands out of — with a network orb that travels across the cut as the same DOM node, which is what makes the two screens one place. Ported from `docs/v6-mockups/coldboot-splash.html` for mechanics; **every number replaced**, because the mockup's hardcoded `chain tip 3729557` is the exact `height.mode` in this repo's own fixture — true when authored, false now, and *a fabricated live value never announces itself as fabricated, it announces itself as plausible*. Four things built so they **cannot** make a false claim rather than being checked for one: Tor/I2P orbit in shells because they have no location to place; the block ripple has no origin because which node found it is unobservable; hostnames are absent from the type rather than stripped; and the orb is a SIBLING of ColdBoot, so "it survives the collapse" is structural — nothing has to remember not to unmount it. The mockup's seven hero passages were **not** all verbatim and passage 3 bylined a four-line quote to a named living person no repo source supports; operator ruled repo-is-truth, and the repo's own convention is role-not-name in all three comparable sites. `verify-hero` therefore asserts **text AND attribution, record-bound** — the file-scoped version PASSED the exact defect it existed to stop (`#174` appears in 5 of 31 records, a 2010 date in 19), proven by running the old logic against the fail case. **Seven instances of one family: an assertion whose subject is wider than its claim** — file-scope vs record; `[class*="grid"]` matching zero elements; a determinism screenshot taking the splash root, then the canvas *element*, before landing on `canvas.toDataURL()` because an element screenshot composites overlapping paint; a vacuity guard labelling a breakdown it never checked and got wrong; §0a proving commit↔disk under a name implying commit↔wire; a freshness check asserting HTTP 200 where the SPA catch-all returns 200 with 41,242 B of index.html; and `verify-vitals`' CPU probe as a validity claim when it measures average throughput and blocking measures tail latency. That last one nearly cost real work: a 479ms blocking figure I called valid was **my own 29-gate chain running beside the measurement** — it never reproduced, reads 309ms on a quiet box and 196ms on an idle one, and the correct outcome was to not change the app. Harness now asserts **commit → disk → wire**, each link with its own verdict, and `verify-vitals` skips as UNVERIFIABLE when run spread exceeds 10% — it fired on its first real chain, `/` at 52.8% with the probe reporting a healthy machine. Also fixed, pre-existing and out of scope: the footer clock baked the build machine's wall time into all 13 prerendered files while every neighbouring figure degraded to an em-dash — the one element on the page inventing a value, and the likeliest to be believed; builds went from 13 differing files to 1, the remainder sanctioned `Math.random()` in `protocols/`. `eagerJsGz` raised 88,000 → 96,000 by re-applying the file's own ~10% rule, with the cost stated beside it: detection threshold 873 B → 8,873 B. Gates 68 → 71 (`verify-coldboot-live`, `verify-coldboot`, `verify-hero`, `verify-orb`); `verify-discrete` had been crashing before any assertion since the sweep landed and now contributes **52**. Final: 29 e2e gates · 0 failed · 2 skipped (both UNVERIFIABLE, machine) · `verify:static` 20 · `verify:bundle` 25 · 7 offline gates · eager 91% · chunks 60/60±4. **Then CI found the one thing the local chain could not**: `healthy · / · CLS 0.0158 ≤ 0.005`, deterministic on two SHAs, unreproducible here in three runs. The hero kicker read `LIVE · MONERO NETWORK · BLOCK 3,700,124` — **327px of one-line text in 334px of box**, seven pixels decided by font metrics, fitting on the authoring machine and wrapping on the runner. It is the only element above the hero's reserved rotator, so one extra line moved the rotator, the CTA row and the live strip together by 14px; the fourth source read as a resize and is not one — shift rects are clipped to the viewport, so the strip at y=725 in 844px reports 119 and at y=739 reports 105. **Four sources, one cause.** Fixed by deleting the block height from the label rather than by moving the ceiling: it is a live figure whose **digit count grows over time**, so any margin there is a countdown, and it was already rendered by the `Height` stat below — which unlike the label carries a provenance badge. One-line need 327px → 176px, with `nowrap` as the belt so a future edit reintroduces an ellipsis rather than a shift. The new check is not a tighter number but a binary property — *this box is the same height whether or not the feed answered* — asserted across the two passes `verify-cls` already loads, so it costs no page load; the `BREAK=same` polarity is the one worth keeping, where both real assertions stayed **green while proving nothing**. Lesson generalised: **any measurement whose margin is smaller than the between-machine variance of its inputs has told you nothing** — the same class as the 479ms, inverted. Final: `/` CLS **0.0000** healthy and degraded · verify-cls 16 → 20 passed · 29 e2e gates green, 0 red, 0 skipped · https://github.com/aqua-019/satoshis-vision-v1/pull/162

- XMRIRISH-20260804-16 · done — v6.1.9 cold boot: three post-merge defects, one live. **The orb had never rendered in the console and 26 green assertions could not have caught it** — `verify-orb`'s only context factory calls `coldBootOff()`, so its SUBJECT was the bypassed Main Home while its CLAIM read as "the orb"; Home draws fine at 538.9×468 and the one broken surface was the one the gate never rendered. Instance ten of the standing family and the first to reach users, and the first recorded case of a subject NARROWER than its claim — the tell differs, because a narrow subject is permanently and quietly green. THREE causes, and the brief's two candidates were both wrong: the canvas wrap collapsed to 0 in a 160px slot (instrumented build logged 3 `resize()` calls, 3 early returns — the early return was correct behaviour given a zero box), so the store stayed at the 300×150 HTML default; and `[data-orb]` painted UNDER ColdBoot's opaque `z-index:1000` root, so geometry alone would have shipped a correctly-sized INVISIBLE orb. Fixed with a slot height, a floor that never binds (120 vs a smallest real allocation of 211.9 — insurance, not a dimension), a one-shot warn, and a z-index scoped to `active`. Gates predict rather than remember: the structural bbox is `2·ORB_RADIUS_FRAC·SHELL[2]·min(W,H)` with both constants PARSED from `orb.ts`, landing within 2px across six contexts; dot-alpha separates a live census from none by two orders of magnitude (94–99 vs ≤1). Break-test: reverting the fix reds 4 of 6, and the two that stay GREEN are exactly "not 300×150" and "the canvas is painted" — both satisfied by a stale buffer, which is the whole argument for the assertion ordering. Frame zero: an opaque pre-paint floor in ColdBoot's own `#050505` (NOT the per-theme `--amb-floor`, which would step colour on indigo), a two-clause predicate proven equal to its inline copy over a 56-row truth table, and a 1.5s hold implemented as a MINIMUM — 1780ms default, 350ms at `hold=0`, 453ms on a dead bundle because the watchdog ignores it. The watchdog removal sits OUTSIDE its `childElementCount===0` branch (unreachable since prerendering); moving it inside reds all three B4b assertions with `#root` at 0 chars. Fit-to-screen: cap `min(2100px, 94vw)` from the mockup's own `.con-grid` (owner CONFIRMED; the briefed `coldbootsplash_6.html` does not exist), weighted 1.00/1.12/1.04 panes, orb stage absorbing slack — console 852px at every desktop width and **2282.6 → 796px on phone, where 63% including the orb had been silently clipped**. Handoff CLS **0.0386 → 0.0000**: the source was `[data-orb]` itself, lerped via left/top/width/height, now transform-driven. `verify-vitals` is RED on 3 blocking budgets — and RED on the unchanged base commit too (`/` 537ms baseline vs 518/436 here, two routes self-skipping at 80–94% spread), so the machine, not the tree; no app or budget change made. Also measured and NOT fixed: `verify-vitals` bypasses the splash, so a real cold `/` is 4048ms LCP against the 2128ms the gate sees — ~1920ms invisible to every gate. Gates 70 → 71 (recounted, both prior figures stale by one); `verify-orb` 26 → 104 with 0 skips · `verify-coldboot` 23 → 29 · `verify-coldboot-live` 22, still 0 skips · `verify-cbpending` 27 · https://github.com/aqua-019/satoshis-vision-v1/pull/new/claude/post-merge-defects-v6-u1rns4
- XMRIRISH-20260805-17 · done — v6.1.10 cold boot: the orb's badge/caption block never faded with the orb (measured 5514ms of text over the decrypt field, 4708ms of it with its own canvas at 0) and 104 green verify-orb assertions could not have caught it — every one ran splash-bypassed, where `assemble` is pinned to 1 and every opacity is 1 by construction; instance thirteen, blindness from the gate's CONFIGURATION rather than its predicates. Fixed by sharing one value with one owner, asserted in verify-orb §7 (which was already standing inside the defect's window un-bypassed, so the prompt's verify-coldboot-live §1d placement was declined — it would have put a 6.4s sampled assertion in the gate that runs first). Instance FOURTEEN found while writing it: §7's Z2 hit-tests with elementFromPoint, which ignores opacity, and was green over a completely invisible orb at BOTH viewports; moved to the console phase and relabelled. Console panes got the mockup's other two vertical growers — dead pane below ENTER 723.9/363.9/195.7/105.7px → 0.0 at 2560/1920/1440/1280 — with the log at `flex:1 1 0` because `1 1 auto` makes the stacked floor INERT (240px with the floor removed, so its own break-test could not red), and a line wrapper because the ellipsis `overflow:hidden` on each row zeroes its flex minimum and crushes 20px lines to 12.5 while scrollHeight===clientHeight reports nothing. CB_HOLD_MS 1500→750 with eight prose sites, the cost sentence re-measured against the no-hold splash-on baseline (~380→~930ms, marginal ~550) rather than the bypassed one. 58/59 gates exit 0; verify-vitals' `/` blocking budget fails on `63dc1a8` too (367/595/487 vs this branch's 458/501/437/394/437/515) and is uncalibrated for this container, and all 29 e2e gates including verify-vitals are GREEN on CI's runner. Independent verification found INSTANCE FIFTEEN, pre-existing and reproduced here before recording: [data-orb]'s baseRef is captured in a race (Orb.tsx:598-600 takes homeRect ?? consoleRect, whichever measured first), so on some cold loads the orb draws a 660x450 buffer into a 551x731 slot through scale(0.835,1.302) — a 1.56x aspect distortion, a visibly elliptical globe, SCALED 1/10 here and 3-5/10 on the verifier's machine. Both gates covering that element are green in BOTH states: one compares the layout box to the backing store, the other the post-transform bounding rect to the slot, and the distortion is in the transform neither reads. Not fixed — the fix is a decision about which rect owns the orb at the console phase · https://github.com/aqua-019/satoshis-vision-v1/pull/164
- XMRIRISH-20260805-18 · done · v6.1.11 orb position: `window` scroll listener never fired above 768px (drift == scroll offset exactly, 400.0px at 1440/1280/900/769, 0.0 at 768/390) and `baseRef` froze on the wrong rect in two independent ways (cold-boot race 1/10 at ratio 0.6411; slot resize during the console phase). Capture-phase listener + rAF coalescing + identity-stable commit; `live` sequences the sources, `travelling` scopes the freeze to the ENTER ramp; `clip-path` bounds the orb to its scrolling column, closing a 61px topbar bleed the tracking fix created. verify-orb §8 (drift, 4 viewports, ≤768 positive control) + §9 (rendered-vs-backing-store aspect over 10 cold loads). Both break-tested. 29/29 e2e · verify:static 21 · verify:bundle · 6 api · typecheck · build all exit 0. PR: https://github.com/aqua-019/satoshis-vision-v1/pull/165
- XMRIRISH-20260805-19 · done — v6.1.11's topbar `clip-path` shipped with no assertion that it works: the only observation of the live value was an `R.info` taken at rest, where the value is the no-op `inset(0px)`, and the file's one `R.ok` about the clip asserted the clip was ABSENT. `verify-orb.mjs` §8 gains three claims per cell — the arithmetic invariant (the shipped `inset()` equals the clip ancestor's rect minus the orb's own box, off two independently measured boxes, via a walk deliberately RE-IMPLEMENTED rather than borrowed from `resolve()`, because "actually scrolling" and "clips overflow" are different questions that happen to share an answer here), the bleed itself, and a positive `no clip-path is emitted` at ≤768 where `.main` is `overflow: visible`. **The finding is that the first version of the bleed assertion was itself vacuous and its own guard caught it**: hit-testing nine points through `.topbar` passed at every live cell and measured nothing, because `document.elementFromPoint` returns only the TOPMOST element and inside that band it is `nav.nav-main`. The `off.hits === off.tested` leg, added so "clean" could not be satisfied by absence, returned `0/9` in BOTH polarities. **Corrected after review, and the correction is the durable part**: the first write-up said hit-testing cannot see this bleed at any sample density, which is false — `elementsFromPoint` (plural) reads 9/9 with the clip off against 0/9 shipped, because `clip-path` removes an element from hit-testing outside the inset as well as from painting. The defect was the singular/plural choice, not hit-testing. Pixels remain right for a better reason: NEITHER form measures paint, both answer "is this hit-testable here", and that coincides with painting under `clip-path` while diverging under `pointer-events: none`, an opaque cover, or a layer promotion. A paint claim is asserted in pixels because hit-testing is a proxy, not because it is blind. Instrument replaced with five frozen captures per cell (orb hidden · shipped · clip off · shipped · orb hidden) compared as differing-pixel counts decoded in-page, with `h1 === h2` proving the freeze held across the window and the rAF stub QUEUEING callbacks so the orb's draw loop survives into the next depth. Skip condition is now read off the same instrument that asserts — "the orb paints nothing here even with the clip off" — after two wrong guards (an 8px overlap floor, then `tested === 0`), both kept in place because the rule underneath took two attempts to state. Cells 4 assert / 6 assert / 1 skip against the 3/6/2 agreed in the ANSWER; the delta is `769@400`'s 3.6px sliver, which the old rows straddled and the pixel read sees as 1820 of 15738 pixels. Break test (`clipAncestor` → null): 9 reds, ≤768 green, skip still a skip. `verify-orb` 191/0 → **223 passed · 1 skipped · 0 failed**; `verify-coldboot` 82/0, `verify-coldboot-live` 21/0, `verify-cbpending` 27/0, `verify-govern` 50/0. No gate added, so the tree stays at 71. Also: two citations that moved when #165 inserted 423 lines, and handoff 18's stale `verify-coldboot` count.

- XMRIRISH-20260810-20 · done — V2·0 the layout system. `--sp-1..--sp-7` declared once and `src/mempool/` + `src/views/` migrated (408 sites measured, not the briefed 288; 300 migrate, 108 stay literal), nearest rung with **ties broken toward the SMALLER rung** because `verify-fit:64` and `verify-memviews:340` have ~2px of overflow headroom and shrinking is the safe direction. `.mp-switcher` moved from `position:fixed; top:61px` into a real `.mp-chrome` row — measured 206×74 at 1440 covering `.mempool-search-bar` on 5 of 6 views, **0 everywhere after** — with the `top:` coupling and both compensating overrides deleted. **The brief's central premise was false and the operator confirmed it**: `labelStep`/`tickCount` ARE adopted, in `markets/charts.tsx`, which the mempool views render through; the brief's grep was scoped to two directories and generalised to "no view imports them" — a subject narrower than its claim, in the prompt that asks you to catch exactly that. Real defects were `charts.tsx:875`'s `|| i === n - 1` (a correct stride defeated by an unconditional final label) and AreaSeries' hi/lo markers never tested against the last-value pill (a descending series rendering its low twice). **The second was caught by the gate this same PR added, after a first fix targeted the wrong pair** — that worker validated against the numbers in its brief rather than against a render and returned a confident self-authored 15/15. New `verify-memviews` scenario 6: AABB over every SVG `<text>` at 4 widths, a declared `EXPECT_SVG_TEXT` map asserted in BOTH directions, and one allowlist entry waiving bridge's gauge caption over its own gauge's value **structurally, by DOM markers, never by text** — the earlier literal pin `'0:41'` was wall-clock derived and produced a real false red ~1 run in 6, and even a regex would leave the subject as "layout AND the current time". Exemption by RELATIONSHIP, not by element: `data-decorative` would stop that caption being compared against anything, forever. **Two process findings worth more than the feature.** (1) A break test must assert its mutation APPLIED before interpreting the gate — the cross-gauge negative printed four ❌ that looked like a clean red while `other-gauge wrappers injected: 0` showed it never applied; the reds were the previous test's damage, and one incidental count was all that stood between that and a recorded pass for a test that never ran. (2) `git checkout --` is repo-state-relative and a break test needs mutation-relative: against an uncommitted tree it discarded `bridge.tsx` entirely, and the script's own check reported `reverted: 0 unexpected entries` — *clean*, which read as success and meant all branch work on that file was gone. **Clean and complete are different claims.** Recovered and proven byte-identical by content hash (`bridge-Vr-RjKfm.js` re-emitted, `cmp` clean) — stricter than a diff, though silent on comments. **§4's labels break test cannot red, and the reason is structural**: `useChartMetrics` measures the pre-transform width and `.mp-view` is `width:max-content`, so the strat log measures ~1180px at every viewport and stride is already 1 with a 14-block fixture; forcing `labelStep→1` is a no-op (mutation confirmed in the bundle by chunk-hash change, text count unmoved at 31). So scenario 6 does NOT cover the BarSeries fix — non-vacuous, reds on a real injected overlap, but that gap is documented rather than papered. Costs measured and accepted rather than absorbed: stage height −5.9/−7.1% at 1440×900, and **−28.4% at 900×500 landscape** where the header wraps; `verify-fit` 84 → 126 of 200. CI found the one thing the local chain could not — `verify-bundle` runs after Build in the build job, outside `verify:static`/`verify:e2e`: `/live/network` **35 B over** a ceiling main already sat at **99.6%** of, whose `measured` comment was stale by 9,169 B. Raised to 108,000 (~1.9%, deliberately NOT the table's ~10% rule, which would re-hide the growth) and the table-wide staleness recorded in place. `verify-shots` priced: **~1.3s/shot, zero noise floor** on `/live/mempool` — far cheaper than anyone assumed, but `?v=` variants are unreachable through `verify-lib`'s ROUTES, so a 6-view matrix needs 5 new entries and belongs with the `networkidle` fix. Used by hand as before/after proof: markets, thesis and network **byte-identical**, only mempool differs. Gates stay at 71; `verify-memviews` 77 → 125 · `verify:static` 7321·0 · `verify:e2e` 1417·0 · `verify-bundle` 25·0 · all 3 CI checks green on `adb4eea`. Unverified by anyone: **no human has seen the rendered result**. https://github.com/aqua-019/satoshis-vision-v1/pull/167

- XMRIRISH-20260810-21 · done · v2·1 sediment hero + two #167 riders · PR from claude/sediment-v2-1-arpfjq @ de82948

- XMRIRISH-20260811-22 · done · v2·2 constellation hero + contract Rev 3. **The sphere is a filled BALL, not a shell, and that is the whole hero**: points on a shell project with areal density ∝ 1/cos θ, which DIVERGES at the rim, so a shell renders as a flat disc with a bright ring — the exact "reads as a disc" failure the brief names. A ball projects ∝ chord length, maximal at centre and zero at the rim, so limb darkening falls out of the geometry once nodes spread across radii (radial depth = real age). Measured: mean alpha 0.654 > 0.236 > 0.096, areal density 160.0 > 46.2 > 11.1. **SVG over canvas, and the reason is not performance**: native hit-testing removes every coordinate conversion from the click path, so no hit-radius tolerance can absorb a coordinate-space bug — §9's defect class, verified by there being no units to get wrong. Every node/radar dot/block glyph now opens the SHARED inspector, which is new functionality: the prior file had no click handler outside the search bar. **A §6 compliance caption breached §8**: unbounded, its 140 chars demanded 981px in a `width:max-content` view and pushed naturalW 1028→1413 past canvasW 1180, handing the view a 0.835 transform and dropping authored 11px to 9.19px rendered; capped, naturalW 1132 and scale 1.0. That conflict is now a contract rule — every v2 view carries a disclosure caption and every one will hit this silently. **The contract's own §6 was FALSE**: it said MemViewShell renders the table "instead of children", copied from a stale docstring at mempool-shared.tsx:339-342 that the implementation comment twenty lines below contradicts; `showBody` consults `tracking` and never reduced motion. Both shell comments corrected. Rev 3 also gains §8 (the fit scale has TWO terms; the height clause fires in `0.92·wS ≤ hS < wS`, ~8% wide, and is exercised by NO gate — measured firing for reactor@1000, bridge@1020, sediment@800, predicted-then-confirmed) and §9 (assertion space, tolerances, **a gate's result is usable only if the run COMPLETED** — verify-perf's recorded 1-failure baseline came from a crashed run and the real figure is 3). **Instance count for the standing family: the sediment amplification was TWO CO-EQUAL DRIVERS**, which single-removal cannot see (hide either: 0-1px; hide both: −2081) and which produces a FALSE NEGATIVE — bounding one column measures exactly zero, so a correct fix reads as refuted. New `verify-memviews` scenario 8, +21 assertions (151→172, method: raw grep minus the summary line), parsing LIMB_K/LIMB_MIN_ALPHA/MAX_SPHERE_NODES out of source and tying its txid check to the app's own shortHash. Three break tests, each proving its mutation applied and restored byte-identical from a snapshot; the reduce one reds by 80 nodes after hardening (the original assertion measured a SUBSET of the mutation's reach — 3 of 62 — because the frozen frame sits a small rotation from the animated sample). typecheck · build · verify:static (21) · verify:bundle (25·0·0·0) · verify-fit · verify-memdetail · verify-reduce all exit 0. design-reviewer APPROVE after one bounded round (its only FAIL — five spacing literals — CORRECTED by multiset diff, not by count). Recorded not fixed: sediment's 3281px, `styles.css:1207` stale in both dimensions for both views it names, HEIGHT_FIT_TOLERANCE's zero coverage, verify-perf's 3 reds including `/` running 179 rAF callbacks in 3s on a hidden tab, and scenario 7's intermittency (1 red in 6 runs, mechanism a candidate not established). https://github.com/aqua-019/satoshis-vision-v1/pull/169

- XMRIRISH-20260811-23 · done · v2·3 Terminal v2 + contract Rev 4. **Three gates were red on the untouched tree and nobody knew, because two of them are in no npm script and no CI** — `verify-perf` (3 failures), `verify-pageshell` (1), `verify-memviews` (1, the known sediment intermittent, which passed on re-run of the same tree). The pageshell red was Terminal's own rail: `styles.css` carried a **bare** `.rail { display: none }` in a 769–1199 band, and `:2149` had ALREADY fixed exactly this with a child combinator and a comment naming `terminal.tsx`. It did not take, because the blocks overlap and `.shell > .rail` simply does not match a non-shell child — so the bare selector won **by default rather than by specificity** and the earlier fix looked applied. **Deleted rather than re-scoped**: `[769,1199] ⊂ [769,1279]`, so both declarations were redundant the day they were written, and re-scoping would leave the same two-identical-rules-in-nested-ranges condition that caused the bug. The duplicate, not the selector, was the defect. Measured two-polarity: rail `{display:none,w:0}` → `{display:flex,w:320}`, gate 367/1 → 368/0. **The failure MESSAGE was also wrong and is corrected** — it read "(.shell > .rail did not over-reach)" while that rule was the one behaving correctly, sending the reader to inspect the innocent party. Wrong-subject shape living in a failure message, which is worse than in a predicate: a wrong predicate is caught by the next reader, a wrong message only by someone already debugging. **DENSITY IS NOW TWO NUMBERS (contract §10), because one is defeatable in each direction**: N (readouts, `verify-memviews` scenario 9, `[data-mem-body]`, non-zero boxes, leaves + `<pre>` lines) went **97 → 351 (3.62×)**, and N alone is gameable by rendering twenty fields fifteen times; M (distinct feed fields, from source) went **M1 20 → 23 / M2 29 → 40**, and M alone is unhittable — `MoneroLive` has ~33 top-level fields, so "3× distinct fields" is unfalsifiable rather than strict. The pair says which happened: the view now renders EVERY field of `Block` (9/9) and `Tx` (8/8). Three chart-kit charts (blocktime intervals vs target · a mempool fee/weight clearing curve with the `blockWeightLimit` crossing interpolated between two real samples · hashrate), all in the MAIN column, all `useChartMetrics` FLUID so **one user unit is one CSS pixel**. **Contract §2's `VB_W` sentence was FALSE and is withdrawn** — `charts.tsx:7` says it "survives only as the pre-measurement fallback", and a fixed viewBox is a TRANSFORM: at 358px render an authored 9.5 paints at 3.4px. Found while checking it: **`MarketsThesisTab.tsx:198` still ships a fixed 1000-unit viewBox on the live `/live/markets/thesis` route** with authored SVG text at 8.5–10px, invisible to two gates for two independent reasons (`verify-legibility` excludes SVG presentation attributes by design; scenario 6 is scoped to `.mem-view`). Recorded, not fixed. `EXPECT_SVG_TEXT.terminal` `[1440,2560]` → `[390,768,1440,2560]`, a **tightening** — those widths move from "must be 0" to "must be > 0". §8's fit apparatus is **inapplicable** (`FitView.tsx:11`; Terminal is `fit: false`) and that is stated rather than silently skipped; the width rule still bit in pan form and **naturalW 1491 → 1180 vs canvasW 1180** removed a pre-existing desktop pan. **Bundle crossed and the prediction was wrong**: predicted +5,000..+9,000 before the build, actual **+15,611** (948,435 → 964,046), entirely the lazy terminal chunk (20,361 → 35,969; eager index +3 B; `/live/mempool` first load green at 97%). Ceiling 960,000 → 981,000 argued against the gate's own arithmetic, not the round number — **the gate WORKED**, it was sized to speak a third through a nine-view roadmap and fired on view three naming one view; what was wrong is the per-view premise (9 × 5,836 from ONE observation; three now measured at 5,836/4,164/15,611, mean 8,537). It still does not make the roadmap fit: 7 × 8,537 projects 42,805 B over the new ceiling. **§5's pan break test is structurally impossible** — `styles.css:2438` pins non-fit views to `width:900px; min-width:900px !important` at ≤768, so scrollW is 900 by CSS fiat and no composition change can move it; the test targets the pin, the real controlling mechanism. Five break tests, each proving its mutation applied and reporting effect size: chartkit 52→51/1 · memviews N **351→263** · pageshell 368→367/1 · mobile 2→1/1 · reduce 31→29/2 (css 1, smil 0). **The harness's own two failures are the durable lesson**: `breaktest.sh` was created with Write and never `chmod +x`, so all five invocations failed "Permission denied" while the runner printed its completion marker **unconditionally** — a total no-op reported as five passes, caught only because the log directory was empty. Then debugging it through `| head` SIGPIPE'd past the EXIT trap and stranded a mutation in the tree, the exact hazard the harness exists to prevent, committed while fixing it. Exit codes are now classified so a harness failure is visibly distinct from a red gate. Also: **§4's counting method is gate-specific** — reporter gates indent their marks so `grep -c '^\(✅\|❌\) '` returns 1 for 128 assertions, and `verify-mobile` has no summary line at all. 179/0 · 39/0 · 31/0 · 128/0 · 2/0 · 368/0 · 25/0 · verify:static 21 · typecheck · build all exit 0. Gates stay at 71 (scenario 9 is a section, not a file). Unverified by anyone: no human has seen the rendered result. https://github.com/aqua-019/satoshis-vision-v1/pull/170

- XMRIRISH-20260811-24 · done · v2·3b the orphan gates — four wired, one fixed. **Four gates existed, asserted real things, and ran in NOTHING**, so `verify-perf` had been red on `main` for three reasons with nobody to say so. Two of the three were defects in the gate: *"un-gated setInterval"* tested ALLOWLIST MEMBERSHIP not gating (both files it named gate correctly, and `verify-provenance` asserts one of them green in the same CI run — the repo claimed both "the clock pauses while hidden" and "the clock has an un-gated setInterval"), and the pre-paint probe read `#root.childElementCount` as "React has hydrated", which prerendering made true from the first byte — **a dead assertion that fails**, passable only if prerendering were removed. The third was real: `ColdBoot.tsx` froze the CLOCK while hidden but kept SCHEDULING, so the ramp never reached 1, the `t >= 1` early return — the loop's only exit — became unreachable, and the same frame repainted forever (**179 rAF/3s → 0**; NO battery claim, a real background tab is rescued by the browser and what this removes is the app depending on that rescue). **The break test measured a blind spot rather than asserting one**: deleting `useNodePopulation.ts:316`'s guard left the gate GREEN at 24/0, rescued by the words `document.visibilityState` in a COMMENT at `:28`; with `stripComments` it reds at 23/1. The old predicate read prose as code, which is why `xmrirish-feed.ts` — no timer at all, one past-tense docblock — needed an allowlist entry to suppress a false positive the predicate itself manufactured; `GATED` now ships EMPTY and self-polices. **My own first fix was racy and I caught it, not a re-run**: `waitUntil:'commit'` then `evaluate()` is a round trip, green five times then `booted=true` on the sixth — **sampling an instant cannot establish an order**, so the entry chunk is now held 1.5s (4 identical runs). **Found while wiring, not briefed: an overflow assertion that CANNOT FAIL.** `html, body { overflow-x: clip }` below 769px means `documentElement.scrollWidth` never exceeds the viewport — a 3000px element appended to `<body>` leaves it at 390 while `main.scrollWidth` reports 3012 — so 3 of `verify-perf` §4's 8 widths and the WHOLE of `verify-mobile`'s sweep were vacuous. Positive controls added; measuring `main.main` instead was REJECTED (overflow-y:auto reads exactly 17px on `/future@1440`, trading a nameable blind spot for an uncharacterised tolerance). Routes: 46 of 68 stale literals cleared, plus `verify-fit`'s 2 which `CLAUDE.md:429` OMITS — its sweep was quote-anchored and `verify-fit` is template-only; confirmed by prediction (a quote-anchored count reproduces the record exactly for all four untouched files, 12/6/3/1) and it predicts two more absentees, `verify-sims` and `verify-v508`. **The stale names were NOT producing wrong answers** — every one resolved to its canonical target, query intact — the defect was an unstated dependency whose failure mode is a silent green. Census corrected IN PLACE: a re-derivation said 14 orphans; `CLAUDE.md:442` already said 11, and 14 − 3 shared modules (`verify-lib` imported by 29 gates) = 11. **11 → 7**, CI files **57 → 61**. `verify:perf` → `verify:perf-classic` (the substring trap that caught two people). Bundle both endpoints BUILT: eagerJsRaw **0 B**, lazyJsRaw **+400 B**. perf 18✅/3❌ → 22✅/0❌/3 skips · pageshell 368 → 369 · fit 21 → 22 · mobile 2 silent → 3✅/1 skip with a summary it never had. typecheck · verify:static (7322·0) · verify:bundle all exit 0. Unverified by anyone: **no human has seen the rendered result**. https://github.com/aqua-019/satoshis-vision-v1/pull/171

- XMRIRISH-20260811-25 · done · v2·4 Reactor — **the artboard was content-derived, and that was the whole defect.** `.mp-view`/`.mp-fit` are `width: max-content`, so reactor's width was whatever the widest ROW demanded and it SWUNG WITH THE FEED: **1668 fixtured / 1413 empty** against canvasW 1180, scale 0.7074, authored 11px rendering at **7.78px** (2.57px at 390). Attribution measured with clone-outside-the-grid max-content demand — a grid item's `scrollWidth` is circular and was not used: row0 `"1fr 380px"` demanded 1628 (block ribbon 1236 + iso 380 + gap), and **the driver differs by feed** (ribbon when populated, hex row when empty), which is why the old number was two numbers. Fixed compositionally: **definite `ARTBOARD_W = 1180`** on `repeat(12, minmax(0,1fr))`, six panels `12 | 8+4 | 4+4+4`. **naturalW 1180, identical fixtured and empty, scale 1.0, 11px renders at 11px.** **The definite width is not an optimisation and break test 1 proves it: removed, naturalW is 3865** — a `repeat(N, 1fr)` grid inside `width: max-content` equalises every track to the widest track's max-content, so the element is `N × (widest) + gaps` regardless of how narrow the other N−1 are. The same structure sits at `sediment.tsx:934`, and a runtime definite width takes sediment **3281 → 1220** — so sediment's long-open width is this mechanism, and the "two co-equal drivers" framing is sharper as **twelve equalised tracks where only the maximum matters**, which is exactly why every single-column fix measured zero. **The headline is qualified because the unqualified version is false on a 1366 laptop**: the floor is met at viewport ≥1440 and ≤1279 and missed across **1280–1439** (9.51/10.31/10.99px) — `styles.css:2149`'s rail breakpoint makes the canvas 259px NARROWER at 1280 and it does not recover 1180 until 1440. v2·4 did not create that band, it REVEALED it; constellation (1132) sits on the same threshold. **Height had to move with the width**, because at scale 1.0 height stops being compressed: naturalH 1201 → **851**, and the pixels came out of CHROME not content — `MemViewShellProps.stats` widened to `boolean | "compact"`, reaching `MemStatStrip`'s compact row, which named Reactor in its own docblock and had been **unreachable since it was written** (strip 66 → 14px). Ribbon, iso stack and ring fan all ended LARGER than the intermediate composition; on screen the view keeps its footprint at **1.42× the rendered scale**. New: a next-block cut, fee-sorted against `blockWeightMedian`, labelled a projection with `source="model"` (a miner's selection is unobservable) and em-dashed when the weight fields are absent. Density as the §10 PAIR: **N 130 → 147** (floor 114), **M 8 → 10** distinct `data.<field>`, the two new ones being exactly the cut's inputs. **Two gate assertions changed, NEITHER because it went red** — each because its subject stopped matching its claim: verify-fit's 100%-toggle pan asserted a symptom the fix removes (relocated 1440 → **1280**; **390 was tried first and is wrong** — `.mp-view--fit` is clamped there so the toggle is inert, and Terminal's 390 pan is a *non-fit* view pinned to 900px, a different mechanism), and `mustScale` predicted a transform from WIDTH while `useFitToView` derives it from width AND height, so a firing height clause printed *"no transform applied"* while one was applied. It now mirrors `compute()` with `HEIGHT_FIT_TOLERANCE` **parsed from source**, and two new viewports (**1440×1000**, 1280×900) actually execute that arm — the first assertion in the repo to observe the height clause working, retiring a listed debt item. `verify-fit:109`'s ≤200 bound was the candidate THIRD change and was NOT taken; the 40px from chrome made it unnecessary. Added `FITS_AT_1440` (both-directions) — **the pre-existing "scaled to fit" assertion PASSED at naturalW 3865 and only the new one caught it** — and `EXPECT_MEMSTAT`, a per-view KEY map (not a count: the two strip branches duplicate five key literals, so the realistic failure is a RENAME, which a cardinality check passes straight through and which would silently drop a figure from item 5's cross-view comparison while staying green). **Two of my own new assertions were wrong on first draft and their own runs caught them** — the 390 relocation, and a blanket `stripCount === 5` that reddened on Terminal, which emits 2 BY DESIGN, a fact `verify-memviews:185` recorded ~100 lines above where I put the constant. A gate that goes red on its author's first draft is a gate. Break tests two-polarity with the mutation proven APPLIED before any red was read, restore owned by a `trap` from a snapshot and rebuilding `dist/` (a `dist/` built from a mutation survives a clean `git status`): fit 46→45✅/1❌→46, memviews 185→183✅/3❌→185. Gates: **fit 22 → 46** · memviews 179 → 185 · mobile 3✅/1skip · perf 23✅/3skip · pageshell 369 · coldboot-live 22 (the brief said 21 — recount, don't increment) · glide 22 · bundle 27 → 28 · reduce 32 · memshell 53 · chartkit 52 · legibility 25, all 0❌; typecheck and build 0. Bundle both endpoints BUILT: **eagerJsRaw 263,456 → 263,456 (ZERO)**, lazyJsRaw 700,990 → **702,407 (+1,417 B)**. Also found, located, not spent: `styles.css:2149`'s `1279 → 1439` closes the type-floor band for every fit view in one line (not taken — it moves layout for every view in that band, verify-pageshell is npm-only, and that block's duplicate at `:2643` is what hid Terminal's rail); **`verify-memstats.mjs` does not exist** though `mem-stats.tsx:158` justifies the whole raw-value design by citing it; the zoom control renders at 390 while unable to change geometry there. Second independent sighting of *"a completion marker must be emitted by the thing that completed"* — `npm run build | tail -4 && echo BUILD_OK` printed BUILD_OK on a FAILED build, a pipeline's exit status being the last command's. **The brief named a mockup, `reactor-v2.html`, that exists in neither the repo nor the upload**; the composition is reconstructed from its span list, which tiles exactly once. Unverified by anyone: **no human has seen the rendered result.** https://github.com/aqua-019/satoshis-vision-v1/pull/172

- XMRIRISH-20260811-25 · addendum (post-push conformance audit) — three gaps closed, two of which the v2·4 brief named and my own compressed spec to the builder dropped. **Two literal font sizes became tokens**: `22 → var(--fs-h2)` (24.48px @1440 — NEARER the mockup's authored 24 than the literal it replaced) and `16 → var(--fs-body)` (14.40px, a deliberate step down, stated in source because the mockup wanted 18 and the six-step scale has no rung between 16.5 and 21; extending a scale `verify-legibility:118` pins verbatim is not a view PR's call). `reactor.tsx` now carries **0** inline numeric `fontSize` against **80** repo-wide. **`dataKey` adopted** on all five panels that read a feed, each mirroring its own `oldestFreshAt` list; the sixth reads no endpoint and correctly carries none. `Stat`/`Pill` considered and REJECTED with the reason stated rather than left silent. **And the find no gate could have made: `MempoolHexGrid`'s cell box shipped at 22×26 while its own comment three lines above said it had been shrunk to 18×16** to match the tightened pitch (y 21→12). At a 12px pitch a 26px cell covers the whole row below it and the backgrounds are `color-mix(…, transparent)`, so they COMPOSITE — the lattice would have rendered as a dark band rather than discrete cells, in the flagship view, on a brief whose subject is that the spacing looks wrong. **It moved no measured quantity, which is why it survived**: cells are `position:absolute`, the container is pitch-driven, and naturalW 1180 / naturalH 851 / N 147 are identical either side of the fix; no gate reads cell geometry. **First instance in this series of "the claim outruns its subject" living in SHIPPED SOURCE** rather than in a gate or harness — a gate can be fixed by widening its subject, source prose has none to widen, and only a person reading the diff would ever have caught it. Collapsed all three hardcoded hex boxes into one `HEX_CELL` constant (+ `PITCH_X`/`PITCH_Y`) so the legend renders the ACTUAL cell rather than a fourth size. New open items: **nothing asserts "inline `fontSize` must be a token"** (`verify-legibility` asserts a FLOOR — sub-14 inline, sub-11 SVG — so any literal above 14 passes, which is exactly how these two shipped green; 80 sites repo-wide is the triage cost); **`verify-memviews` item 4's `timeout: 25000` sits below the schedule it waits on** — it waits for `conf0 + 2` on the 15s CHAIN tier, so two confirmations can need two polls (~30s), and its own comment does the arithmetic for ONE poll then stops; the tier is CLIENT-side scheduling, so stubbed routes and the pure-fixture `advanceBlocks` remove server latency but not the cadence. Correction: 35000 WITH the arithmetic, a bound raised to exceed its dependency rather than a tolerance widened to silence a red. **Whether sediment is disproportionately affected is UNMEASURED, and the first account of it was checked and is FALSE on both halves** — `VIEWS` is registry order so sediment is THIRD of six, and `:218-219` resets `head` and opens a FRESH PAGE per view, so no poll phase carries across iterations and a reversed-order test would prove nothing. The settling test is elapsed time from page load to `advanceBlocks(2)` per view. **Scenario 7 is SEPARATE and still unexplained**, now with two sightings (#171, #172) on trees where sediment was untouched both times — two reds on unmodified source points harness-side, opposite to the current `canvas scale` candidate; confirmed intermittent here, the failing tree passing twice on immediate re-run (185 ✅ / 0 ❌ both). Also recorded: `verify-memviews`' assertion count varies run to run (186 once, 185 twice, same tree, all green). Gates after: fit 46/0 · memviews 185/0 · legibility 25/0 · provenance 38/0 · failure 17/0 · resilience-dom 30/0 · typecheck 0 · build 0.

- XMRIRISH-20260812-26 · done · v2·5 Ops Bridge — **the artboard was one prose node wide.** naturalW 2517 against a 1180 canvas, so FitView scaled the whole view 0.468812 and authored 11px painted at 5.16px; now **1180 in BOTH feed states at every viewport, scale 1.000000, 11.00px**. MEASURED before the grid was designed, with the two instruments contract §8 prescribes: hiding each composition row moved naturalW only for the pool-attribution row (2517 → 1360), and a clone taken OUTSIDE the grid (`position:absolute; width:max-content`, allocation-free — a grid item's own scrollWidth is circular) put its §6 disclosure prose at **1174px**, which its `1fr 1.1fr` tracks amplified to `1174 + 1291.16 + 12 + 40 = 2517.16`, exact. Second order once that row is hidden: gauge bank 638 through `1.05fr 1fr` → 1360, exact. **The brief's named candidate — the `repeat(7, 1fr)` stat strip — contributes ZERO** (hiding it leaves 2517; its widest cell would amplify to 998): right mechanism, wrong row, and only measurement separated them. Fixed with reactor's `ARTBOARD_W = 1180` + `minmax(0, 1fr)`; **break-tested, and the break test earns its keep twice** — removing ONLY the explicit width returns naturalW to 1394 empty / **1430 fixtured**, so the capped captions and `minmax(0,1fr)` are *not sufficient* alone AND naturalW becomes feed-dependent again, a property invisible to any single-feed measurement. naturalH 1856/2300 → 1229/1360, inside the reactor-only `2 × canvasH` bound, canvas showing 51.6%/screen up from 37.8%. **Four defects found by opening a screenshot, none of which reds any of the 71 gates**: a log fee-VALUE radar radius put half the pool inside r/R 0.23 as one clump (now equal-area rank, `r = R√(rank/N)`, uniform by construction at any pool composition); tier rings landing at their own quantiles collided at 146.4/141.4/118.9 (now fixed quantiles carrying live values, so non-overlap is structural); a cadence "target line" that marked the BASELINE while its comment claimed the target; and a 142px ragged bottom edge because a grid item stretches but a `.panel` inside it does not. **And one defect the eye could not catch that the gate did** — `verify-gpu` on `transition: width` for the gauge bar (→ `transform: scaleX()`); neither channel is sufficient alone. `FITS_AT_1440.bridge` false → true with the gate's own "now UNDER budget" red quoted. `verify-memshell`'s 200–1084 band failed at 1265, so the two instruments moved to `bridge-instruments.tsx` — the remedy its failure text names — leaving bridge.tsx at 871; **the VIEW got bigger (708 → 1228), not smaller, and the PR says so**. N 100 → 115 (floor 88), M 16 → 18 (+blockTarget/peerCount/randomxSeedHash/txCountTotal/version, −the three MARKET fields). eagerJsRaw unmoved at 261,392; lazyJsRaw 705,264 → 711,561, 8,439 B under the ceiling, budget untouched. All seven gates green at both endpoints. Recorded not fixed: **`FITS_AT_1440` is an EMPTY-FEED claim** — constellation is declared `true` at 1132 empty while scenario 9 measures 1510 for it under the fixture, i.e. a view can be "under budget, as declared" and 330px over the canvas on real data; the pre-rebuild bridge is a **fourth** instance of the height clause contract §8 calls ungated (fires at 1280×900 empty, hS 0.385291); scenario 6's one bridge waiver is now UNEXERCISED and deliberately left in place; and `claude/FINDING-maxcontent-grid-amplification.md`, which the brief cites, **has never existed in this repo**. https://github.com/aqua-019/satoshis-vision-v1/pull/173


- XMRIRISH-20260812-27 · done · p2·7 Orbital — **the first net-new view of the eleven, and the first hero surface that is neither scaled nor panned at any viewport.** naturalW == canvasW in BOTH feed states at 1440/1280/390 (1180/1020/366), so there is no `.mp-fit` transform and authored 11px renders at 11px including on a phone — the outcome the standing mobile finding has wanted since v2·1. **It was not free and `minmax(0,1fr)` did not buy it**: the first build measured naturalW **2076** against a 1180 canvas, isolated with §8's two instruments (hide-each-cell, then clone-outside-the-grid because a grid item's own scrollWidth is circular) to the Hub cell's 673px demand amplifying 12×673/4 = 2019. `minmax(0,1fr)` bounds a track's automatic MINIMUM; the amplified quantity is the max-content CONTRIBUTION — reactor/bridge are safe because their grid width is DEFINITE, not because of the minmax. Fixed with `contain: inline-size`, chosen after measuring four candidates. Identity, stated against #173's radar on all four axes: motion is the medium (bridge's blips are static under a sweep arm; here the txs orbit and position is a pure function of feed+wall-clock), radius is a fee VALUE not a RANK, bearing is `age mod blockTarget` so arrival bursts read as spokes where bridge's `hashToUnit` angle carries nothing, and canvas not SVG. **The clock is `Date.now()`, deliberately not `useMemCanvas`'s elapsed `t`** — `t` freezes while hidden, and a bearing derived from real age must resume correct. **Two gates the spec has required since v6.0.3 and which did not exist now do**: `verify-tracking` (61) and `verify-memstats` (36), both registry-driven so the remaining four views are swept the day they register, both wired to npm AND to CI as individually-answerable steps. `verify-memstats` makes `mem-stats.tsx:184-186`'s citation of it true after two years. **Neither duplicates verify-memviews**: scenario 2's fixture serves the tracked tx `confirmed: true` from the first request, so "persists across the mempool → block transition" was checked by a test that never saw the transition; verify-tracking drives both states and adds the positive control scenario 2 has no room for. Six two-polarity break tests, trap-owned restore covering source AND dist: T1 61→54/7, T2 61→33/14, M1 36→34/2 (and it LOCALISES — four keys stay green), **M2 36→35/1 with `[median] identical across 6 views: 433732` GREEN and only the arithmetic anchor red**, which is the "every view identically wrong" case parity is structurally blind to, M3 36→22/14 with `identical` GREEN on empty strings, N1 128→120/8. **N1 caught a false negative in my own harness first** — the mutation matched line 28's `MEMPOOL_VIEW_IDS` instead of line 166's switcher, so the gate stayed 128/0 and the derived assertion looked vacuous; v2·0's shape exactly, one step from being written down as fact. **A tripwire no brief listed: `verify-nav` §6 hardcoded SIX switcher tiles** in five assertions plus a literal cascade string and sits in `verify:e2e`, so a seventh view reddened 8 assertions on measurements that were all CORRECT — and the `&&` chain then stopped the TEN gates after it from running. Derived from the registry now, the property `verify-memshell` already asserts of the app. **This PR has a second subject: the shared ≤768 layer**, where registering `reflow: true` exposed three defects, two PRE-EXISTING. The headline is **Classic, the DEFAULT phone view**: the reflow collapse hit `MemTxTable`'s inline grid, rendering a 61×6 table as 366 stacked cells — `.mem-table` 10,876 → **1,965**, document 13,529 → **4,618**, cols 1 → 6. The tell it was never intended: the two rules at `:2488-2489` that exist to let that table swipe **could never work**, a grid collapsed to 1fr having no columns to preserve. And `.main canvas { height: auto }` at ≤768 pins every `.mem-canvas` to the HTML default's 2:1 via its backing store — **sediment 1248×624 → 1248×772** in a 772 host, 19% of its instrument blank. Both fixes measured before AND after by removing them again. Blast radius closed: `.mem-canvas` has exactly two mounts, `reflow: true` exactly two views, nothing reaches markets or network. **Four defects found by LOOKING, none of which reds a gate**: a fabricated `120s` block target on three surfaces (the fallback rendered as a reading; empty-state digit-bearing nodes 8 → 5), the canvas aspect pin, ring labels colliding at 390 (fixed with `spreadLabels`, the repo's own separator), and **my own probe fixture using `fee_estimate_tiers` where the mapper reads `fee_tiers`** — so `data.feeTiers` was empty and the tier rings silently did not render; I nearly recorded that as a code defect, and it was in both new gates too. **A defect in my own gate, caught by its second run**: `oldest` read 1748 vs 1747 because views are read sequentially across a second boundary. The tolerance fix was REJECTED on §9's own rule and the wall-clock TERM removed instead (`page.clock.setFixedTime`), so the anchor is now exact equality with no tolerance. Split at #173's seam when `verify-memshell`'s band was reached at 1126 — taken even though the check is `owned: false` for a new id and would not have failed; orbital.tsx 560 + orbital-instruments.tsx 627, both in band. Budget raised LOUDLY once: red quoted at `❌ lazy JS 733457 ≤ 720000`, final `✅ 733772 ≤ 736000`, margin **2,228 B**, eagerJsRaw **UNCHANGED at 261,392**, and every byte attributed including a chunk nobody wrote — `useMemCanvas` hoisted to a SHARED chunk (+2,763) as sediment shed 2,546, net +217, confirmed by rebuilding 1887edc in place rather than quoting a report. N 150 (floor 132) · M 9 · fps p5 17 vs a 30 bar **FAIL, reported not hidden** (sediment, untouched, reads 7 and its 6/5/7 drift across three runs sets the ±2 noise floor). `verify-vitals` characterised idle on the operator's pre-committed protocol: **branch GREEN, 397ms then 392ms against a 400 ceiling**; the earlier 401/407 were contention and nothing was widened. All 29 e2e gates, static, fit, mobile, cls, pageshell, memshell, bundle green on the final tree. **No human has seen the rendered result in a browser.** https://github.com/aqua-019/satoshis-vision-v1/pull/174

- XMRIRISH-20260812-28 · done · p2·7b Orbital nav visibility — **the seventh view had shipped and no nav surface knew.** `ia.ts` hand-copied six view ids and a literal `h: "Mempool · 6 views"`; `orbital` landed in #174 and was absent from the mega-menu, ⌘K, prefetch and breadcrumbs, reachable only from the on-page switcher (registry-derived, therefore right) and a typed URL. **The wall behind the copy was real, and removing it — not adding a seventh entry — is the fix**: `views/index.tsx` is a `.tsx` and bare Node's type-stripping cannot load that extension at all, so `ia.ts`, which `verify-ia` imports under bare Node, structurally could not read the registry. `views/mempool-meta.ts` is the component-free half, imports NOTHING, reached through `nav/registries.mjs` — the pattern `protocol-meta.ts` already established. `index.tsx` binds components through `Record<MempoolViewId, ViewComponent>`, a union off the `as const` array, so a view with no component is a COMPILE error (the `Record<ProvSource, …>` idiom). **The constraint no brief anticipated, and the reason this is not a one-line change: SIX gates parse the view list out of `views/index.tsx` with one shared instrument** (`/\{\s*id:\s*"([a-z]+)"/g`). Two would have gone red on a bare re-point; four SWEEP inside `for (const id of VIEWS)`, so an unparseable file is not automatically a red. **I wrote "all four go vacuously green" into a commit message and it was wrong — read off the code, not run.** Measured counterfactual (registry emptied AND floors removed): **verify-memperf EXIT 0 printing an EMPTY table under `✅ every canvas view holds ≥30fps at the 5th percentile`** — a green claim about zero views — and **verify-tracking EXIT 0 at `0 passed · 0 failed`**; but verify-memstats reds on an INDEPENDENT guard (its declared-key both-directions check) and verify-memviews CRASHES rather than asserting, an out-of-loop `VIEWS[0]` becoming `data-mem-view="undefined"` — zero assertion lines, no tally, a stack trace. So the floors are load-bearing for TWO and defence-in-depth for two, and they still earn their lines on the rest: memviews' turns a stack trace into a named red, memstats' replaces three reds that describe the WRONG CAUSE ("a dead key is a rename nobody noticed" when the gate never opened a page). Corrected in e857ad1 — the overstatement is the same defect this PR is about, a claim whose subject outran what was measured, and it reached a commit message before it was checked. Each gained a non-vacuity floor; `verify-nav`'s is the sharpest, because at `N_VIEWS = 0` its expected tile sequence and cascade both become the EMPTY STRING, which a switcher rendering nothing would MATCH — the gate would have confirmed the switcher correct by confirming it absent. **`verify-ia` §7b (7 assertions, 23 → 30) compares a RUNTIME ia against a SOURCE-TEXT registry, deliberately asymmetric**: ia now derives from the same module, so runtime-vs-runtime would be a tautology green forever after someone re-hardcodes the list. §7 could never have caught this — it strips query strings, so all seven `?v=` leaves reduce to one `/live/mempool` and view membership is invisible to it, not weakly but exactly. **Six-mutation two-polarity matrix, trap-owned restore verified clean.** M1 reproduces the actual #174 defect and reds `1 registered view(s) MISSING from the nav: orbital`. **The brief's own prescribed break test does not apply as written** — "remove one view from the metadata → verify-ia reds (item and count)" presumes ia keeps its own list; under a real derivation that mutation keeps ia and registry in sync and reds the COMPONENT-BINDING leg instead (M2, measured). **Three corrections to the brief**: `home/sections.ts` does not import the registry, it derives from IA, so it was transitively wrong and self-corrects; the LIVE section badge is a fourth derived consumer nobody listed, measured **14 → 15**; and the six-gate blast radius was unlisted. Sweep across `src/` + `scripts/`, decommented, both quote-anchored AND template-interpolated: exactly ONE code list of view ids remains (the new module), ZERO code count-literals, 3 template `?v=` sites all derived. **13 stale PROSE counts reported not fixed** (MempoolPage ×3, styles.css, routes.mjs, classic, terminal, useUrlState, useMemCanvas, chart-kit …) — out of subject; the one exception is `styles.css:2553`, fixed because it named the type whose declaration moved AND said "Classic only" when #174 made Orbital a second `reflow` member. Budget: **all three ceilings UNTOUCHED**, eager 261,392 → **261,782** (+390), lazy 733,772 → **733,233** (−539), total 995,164 → **995,015** (−149), attributed to the byte by extracting the minified fragment at BOTH endpoints — eager literal 205 → 570 (+365) plus 25 plumbing; lazy array 1,380 → 804 (−576) plus 37 derivation. The metadata is now stored ONCE instead of twice. CLAUDE.md census recounted per its own rule: 75/71 → **77/73**, and the Current Status line's three numbers had each drifted independently ("57 of the 71 … 3 … 11" → **62 of 73 · 4 · 7**), the 11 contradicting the Orphaned-gates entry below it which said 7 and listed exactly the 7 a measurement finds. **The CI count itself caught two bad measurements before it was written**: 64 (matched gate names inside YAML comments) and 56 (missed all six `api/` gates because the regex assumed `node verify-` and CI writes `node ../api/verify-`); their disagreement is what exposed both. Renders LOOKED AT: mega-menu "MEMPOOL · 7 VIEWS", palette "LIVE · MEMPOOL · 7 VIEWS", seven views each including Orbital.
 https://github.com/aqua-019/satoshis-vision-v1/pull/175

- XMRIRISH-20260812-28 · addendum (rider, operator-approved) — **verify-memviews scenario 7's settle poll, the sentinel PR-172 filed to "ride the next verify-memviews change".** This was that change and the flake recurred on the verifier's clone as `canvas scale 0.9999` — a NEW signature against the historic 0.913x cluster, and the tell: #172 closed the MID-flight window and left a PRE-flight one open. The poll had no pre-wait, so its first read was at t≈0; if the `.mp-fit` spring has not STARTED within the ~60ms between reads, two identical PRE-TRANSFORM reads satisfy "two agreeing reads" and the click maps at scale ≈1.0 against a view rendering at 0.36. `verify-fit:199` carries a 120ms pre-wait for exactly this. **AGREEMENT WAS THE WRONG PREDICATE** — two reads can agree on the wrong value at BOTH ends of a transition, which is precisely why closing one window left the other open — so the poll now converges on a DERIVED expectation, `min(1, canvasW/naturalW)`, which neither window satisfies. **Measured before the assertion was written, not after**: canvasW 1180 · naturalW 3280 · derived **0.359756**, `.mp-fit` DOMMatrix.a **0.359756 EXACT**, canvas ratio 0.359703. **A first cut compared the RATIO and was wrong in an instructive way**: the ratio lags the transform and is not exact, so it needed eps 0.005 — and that run stopped at **0.364549, still mid-settle, and called it settled**. A loose eps on a lagging quantity is an early exit by another name: the same defect as the poll it replaced, wearing a derived expectation. The transform is exact AND leads, so converging on it waits the motion out. Two-polarity: mutating the derivation (×2) reds with `.mp-fit 0.359756 vs derived 0.719512`, exit 1; restored, three full runs **211 / 211 / 210 passed · 0 failed** (the count variance this gate already has on record). **And the break test itself failed once, on this repo's own rule**: the trap-owned `git checkout --` restore ran while the rider was still UNCOMMITTED, wiping it, so the "+" polarity measured the PRE-rider gate — caught only because the settled-line grep came back empty on a run that had to print it. Committed first, then re-tested. Also recorded, not fixed: the pre-existing comment's numbers are ALL unsettled — passes at 0.8326/0.9768/0.9999 as much as fails at 0.9135–0.9139, against a settled 0.359756 — so the old poll was never distinguishing settled from unsettled, only "unsettled where the click still landed" from "unsettled where it did not". Gate-only change; no `src/` file touched, so no byte can move. https://github.com/aqua-019/satoshis-vision-v1/pull/175

- XMRIRISH-20260813-29 · done · p2·8 Abyss — **the eighth mempool view, the second net-new one, and the only SUBTRACTIVE tracked idiom in the app**: fee is LUMINOSITY, age is DEPTH, and tracking keeps one particle lit while dimming the rest. Identity designed against TWO adjacencies rather than one — sediment encodes fee as a vertical STRATUM, orbital as a RADIUS, so a third positional fee axis would have been one of those rotated; fee left geometry entirely for the brightness channel, which makes the next-block cut a BRIGHTNESS THRESHOLD instead of a ring you can point at, and the horizontal axis carries nothing and says so (`hashToUnit`, bridge's own idiom, named as the axis orbital deliberately does not reuse). Per-frame cost is a CONSTANT, not a function of pool depth: alpha and colour are both pure functions of an 8-step luminosity bucket, so the whole pool is ≤8 `fill()` calls — 240 txs is eight, 2,400 would also be eight. Fluid worked first try, orbital's mechanism verbatim: all six `naturalW == canvasW` cells YES (1440→1180/1180, 1280→1020/1020, 390→366/366, BOTH feed states), no `.mp-fit` anywhere, authored 11px rendering at 11px including at 390. **THREE DEFECTS FOUND BY LOOKING, none of which any gate sees**: the LOW-POOL state read as an EMPTY PLOT (at 3 txs the ladder degenerated to `[now, 30s]` plus a floor tick 1s below it, and 1.4px dots were specks in a 735×574 box) — fixed with four fine rungs, a floor-tick suppression rule and a dot-radius LEGIBILITY FLOOR raised uniformly so every size ordering is preserved; the tracked marker sat exactly on the "5s" axis label AND drifted ~19px from its own dot between polls, because the particle sinks continuously while React re-renders every 3s — fixed by giving the draw loop the marker's DOM node (`Orb.tsx`'s pattern, contract §5's "mutate refs or DOM attributes" half); and a c4 panel header wrapped to three lines, **checked against orbital before fixing and orbital does exactly the same**, so the wrap is pre-existing shared `PanelFrame` behaviour and only the title length was mine. **The first floor-tick threshold was wrong in a way only the render showed**: 0.07 dropped the redundant "31s" at maxAge 31 and ALSO dropped "29m" at maxAge 1747, leaving the deepest 5% of the main case unlabelled — a rule tuned on the edge case breaking the common one; 0.03 keeps both. **TWO budget raises, both pre-authorised, both red-then-green on the FINAL tree**: `lazyJsRaw` 736,000 → **759,000** (756,809, margin 2,191), `totalJsRaw` 1,000,000 → **1,021,000** (1,018,671, margin 2,329), every byte attributed by a file-by-file dist diff PAIRED BY MULTIPLICITY — abyss +23,443 lazy · index/mapDeps +133 lazy · eager entry +80 — a clean two-term lazy delta where p2·7 had four, because `useMemCanvas`'s shared-chunk hoist already happened and all seven pre-existing view chunks are byte-identical across the two builds. **The near-miss worth reading: the delta was measured, the ceilings written from it, and then three more rounds of render-driven fixes landed** — the chunk moved 23,115 → 23,443 and both comments would have shipped describing a tree that no longer existed; the reds were re-demonstrated on the final tree rather than quoted from the first. **`eagerJsRaw`'s BUILT value moved +80 where the brief said it must not** — structural, not drift: #175 put the view metadata in an eagerly-bundled module so `nav/ia.ts` could read it under bare Node, so every view now costs the eager bundle one row; the CEILING is untouched. **`totalJsRaw`'s own stated construction ("the sum of the two real budgets") was already broken before this PR** — exactly true at 280,000+720,000=1,000,000, lapsed silently at #174 — and 1,021,000 does not restore it, so a build can sit inside both real budgets and still red on the backstop; raised per policy, reconciliation recorded as its own decision. **`verify-vitals` red is PRE-EXISTING, measured against `origin/main` rather than assumed**: both trees exceed the 400ms blocking ceiling on `/` and `/live/markets`, per-route median blocking is branch/base 414/413 · **261/294** · **463/488** · **311/318** so the branch is better on three of four, `/live/mempool` is green on both, and inside the full e2e chain markets LCP read 4460ms against 2240ms standalone-idle. Gates **77 files / 73 gates RECOUNTED and unchanged** — this PR adds no gate; it fills three existing per-view maps (five keys · `[]` at all four widths · 198 measured → floor 174 at the documented ~12%) and one ROUTES entry (44 → 45). Three-way break test in ONE run, each red localised and none masking another, trap-owned restore verified clean, re-run green at 233. Registration otherwise free: `verify-tracking` **62 → 71** and `verify-memstats` **37 → 38** swept abyss with zero hand edits. **`verify-nav` stayed at 129 and should have** — it derives `N_VIEWS`, so a new view changes its SUBJECT (8 tiles) not its COUNT; a rising count there would have meant a hardcoded list. Four-layer sweep clean, and it found a FOURTH per-view map the brief did not list (`verify-fit`'s `FITS_AT_1440`), correctly needing no entry because Abyss is `fit: false`. `verify:mem:perf` p5 **18 fps** vs a 30 bar — FAIL, reported not hidden, and the best of the three canvas views (sediment 6, orbital 15); bar untouched. All 28 other e2e gates, static, fit, mobile, perf-runtime, pageshell and bundle green. **No human has seen the rendered result in a browser.** https://github.com/aqua-019/satoshis-vision-v1/pull/176

- XMRIRISH-20260813-30 · done · p2·9 Pulse — **the ninth mempool view, the third net-new one, and the first that draws the FLOW rather than the STOCK.** Every shipped view arranges the pool's current contents by some pair of axes; this plots how fast transactions are arriving over real time, so the stock is the integral of what it draws. **TIME IS THE HORIZONTAL AXIS — the first real one in the suite — and that gives this view the only FUTURE in the app**, the segment right of `now` ending where the next block is due. Identity stated against each adjacency rather than claimed wholesale: against ORBITAL, its age is a BEARING (cyclic, no origin, no direction) where this is linear and directed; against ABYSS an exact inversion (abyss puts age on the vertical and takes fee out of geometry into brightness, this puts time on the horizontal and puts fee back in); against SEDIMENT it **SHARES fee-on-vertical and that is said plainly rather than dressed as a clean nine-way inversion** — the separations are that sediment's x is weight and its fee is a STRATUM (a position) where this is an AMPLITUDE (a magnitude hanging off a timeline). **The brief asked for "one axis inversion against ALL EIGHT" and that is not achievable; asserting it would have been this repo's own narrower-subject failure.** History source, the honesty decision: the pool's own age distribution — every pending tx's `age` IS its arrival time, so one snapshot yields the whole series, NODE-provenanced, complete at t=0 — with SURVIVOR BIAS as its caveat, carried on the stage caption in as many words ("arrivals still pending", older end under-counts, the fall-off is the mining process not a gap). Session-log rejected for being empty at load and for its per-tick cap; **the brief mis-stated that cap** — `useFeedEvents.ts:26` is `TX_EVENTS_PER_TICK = 8`, the `40` below it is the ring buffer's total size, two different quantities conflated, and the number was the argument against the option it was used to reject. Both named dependencies are STRUCTURAL via abyss's parameter discipline (`usePulseField(data, oldestAgeSec, etaSec, innerW)`): `oldest` picks the window rung off a **block-target ladder** (rungs are multiples of the node's own `target_seconds`, so a literal 600 is never written), `eta` is the future edge and its marker sweeps toward the present in real time, REMOVED rather than parked on `now` when the tip goes overdue. Composition is two traces hinged on one axis: above, one equal segment per arrival stacked per bin so a column's height is exactly the count and bursts read as spikes; below, one mark per arrival at its own log fee/byte where the inferred cut becomes a horizontal threshold the marks visibly cross. **Two compositions discarded, one by measurement**: per-tx strokes alone make a cheap burst a dense band of SHORT strokes (a smudge, not a spike), and fee-weighted height fails the other way since on the gate fixture one dear tx carries ~180× a cheap one so a single whale out-spikes a thirty-tx burst. Per-frame cost is a CONSTANT — ≤4 fills per trace batched by tier, so 240 txs is eight fills and 2,400 would also be eight. Fluid third time, worked first try: all six `naturalW == canvasW` cells YES (1440→1180/1180, 1280→1020/1020, 390→366/366, BOTH feed states), authored 11px at 11px including 390. **FIVE DEFECTS FOUND BY LOOKING, none gate-visible — each rendered, carried correct numbers, overflowed nothing**: (1) **`tMax` was the live ETA, so the due marker sat at the axis maximum BY CONSTRUCTION** — it could never move, the sweep the view was designed around did not exist, and the domain rescaled every frame so every tx drifted for an unrelated reason; fixed at one block target, exact rather than chosen since `eta = target − sinceTip` with `sinceTip ≥ 0`. (2) The ladder's bottom rung was ¼ block target, so a 3-tx pool got a 30s window against a 120s future — 80% empty future and "+2m" printed three times running; floored at one full target, and the sparse state now reads "one block of arrivals behind, one block of waiting ahead". (3) `u` is 0 at the pool's cheapest and 1 at its dearest BY CONSTRUCTION, so without an inset the cheapest mark was drawn ON the time axis and the dearest ON the floor rule — not an edge case, every pool has both, so two marks were ALWAYS invisible and at 3 tx it was two of the three. (4) A rate ceiling of 1 made one arrival a full-height bar, and quartering a round ceiling is not round (50 → 13·25·38·50); floor of 4 and ticks now pick a 1/2/5 STEP. (5) Both floating labels shared the top row and collided whenever a recent tx was tracked — the common case — and the due label clipped to "block du" at 390; it moved into the FUTURE BAND, the one region where nothing is ever drawn. **One further fix from reasoning, same family**: `plsX` clamps, so an arrival rolling past the left edge between polls was PAINTED ON THE BORDER at a time it did not arrive — guarded in the draw loop AND the hit test, which must agree or a click resolves to a mark the stage stopped painting. Registration fills three existing per-view maps (`DENSITY_FLOOR` 154 measured → 135 at the documented ~12% · `EXPECT_SVG_TEXT` `[]` VERIFIED 0 at all four widths · `EXPECT_MEMSTAT` five keys) and one ROUTES entry (45 → 46); **`FITS_AT_1440` correctly needs NO entry** — fit-only map, this view is `fit: false`, absence reasoned not overlooked. Registration otherwise free: `verify-tracking` **71 → 80**, `verify-memstats` **38 → 39**, zero hand edits. **`verify-nav` stayed at 129 and should have** — it derives `N_VIEWS`, so a ninth view changes its SUBJECT (9 tiles) not its COUNT. **TWO budget raises, both red-then-green on the FINAL tree** (re-measured after the last src edit per p2·8's near-miss rule; the first build read 638 B less, a drift that would have sat inside the margin unseen): `lazyJsRaw` 759,000 → **790,000** (787,894, margin 2,106) and `totalJsRaw` 1,021,000 → **1,052,000** (1,049,839, margin 2,161); `eagerJsRaw` ceiling UNTOUCHED, its +83 structural (p2·7b's eagerly-bundled meta row). **The multiplicity trap was LIVE in this delta rather than hypothetical**: the stem `index` really holds two chunks, they moved by DIFFERENT amounts (+133 and +83), and **one is lazy while the other is eager** — so a basename-keyed diff does not merely lose a row, it charges a lazy delta to the eager budget. All eight pre-existing view chunks byte-identical; 9-view mean recounted to **24,977** (span 19,163–35,969), with Circuit certain and Relay parked so the projection is a floor for one and a guess for the other; `totalJsRaw`'s lapsed backstop identity left OPEN deliberately. **Rider taken**: `verify-memshell`'s `NEW` had kept SHIPPED orbital and abyss since #174/#176 so their line-count bands ran as warnings — both moved to `SHIPPED` and are now owned (561, 579 in band); `pulse` stays in `NEW` until it ships. Gates **77 files / 73 gates RECOUNTED and unchanged** — this PR adds none. Full suite green on the final tree: `verify:static` 21/21, **`verify:e2e` 29/29 exit 0 with zero failures** (including `verify-vitals` 17 passed, which p2·8 recorded as a pre-existing red — it is runner-dependent), `verify-tracking` 80, `verify-memstats` 39, `verify-bundle` 27, `verify:fit`, `verify:mobile`, `verify:perf-runtime`. `verify:mem:perf` p5 **24 fps** vs the 30 bar — FAIL, reported not hidden, joint-best of the four canvas views (sediment 10, orbital 24, abyss 23); bar untouched and the ranking is runner noise. **A NEW HARNESS-LIES-TO-ITSELF INSTANCE, and the mechanism is worth the entry: `| head -N` SIGPIPEs a probe that writes files.** The render probe was killed after printing its first table and BEFORE any screenshot was written, so a round of fixes was judged against PNGs five minutes stale and read as "the fix did not take" — and the six-cell numbers in that same run WERE fresh, because they print first, which is exactly what made it convincing. Caught by timestamping the artifacts against the build. **No human has seen the rendered result in a browser.** https://github.com/aqua-019/satoshis-vision-v1/pull/177

- XMRIRISH-20260814-31 · done · p2·10 Circuit — **the tenth mempool view, the fourth net-new one, and the last buildable one: after this only Relay remains and it is parked.** Its coordinate is a SUM OVER OTHER TRANSACTIONS and it is the only one in the suite that is. **THE LANE IS A CATEGORY, NOT A COORDINATE** — that is the whole separation from the two views that already own a fee axis: sediment encodes fee as a CONTINUOUS vertical stratum and orbital as a CONTINUOUS radius, while a bus lane is one of the node's own four `get_fee_estimate` tiers and within a lane position encodes NOTHING about fee. `CIR_LANES` is `FEE_TIER_LABELS.length`, so no literal 4 is written anywhere. **THE ALONG-TRACE AXIS IS QUEUE DEPTH** — cumulative weight AHEAD in the fee-sorted fill, right-to-left from 0 at the chip's pins, so moving toward the chip is moving toward inclusion and the die's left edge is the inferred cut. **A PACKET'S EXTENT ON THE AXIS IS ITS WEIGHT**, so the pool TILES the axis and occlusion between packets is STRUCTURALLY IMPOSSIBLE rather than managed — impossible beats managed, and it is the answer this view gives to the density problem every other canvas view solves by bucketing. **The identity claim needed stating PRECISELY because the loose version is false**: orbital, abyss and pulse all normalise a fee against the pool's observed [lo, hi], so the pool does affect where a mark lands — but only as a SCALE, and under any renormalisation two equal-fee transactions coincide; here they cannot, because their positions differ by the weight of everything between them. Cumulative, not rescaled. **The staircase is REAL and is stated rather than dressed up**: `feeTierIndex` is monotone in `perB` and the fill is fee-sorted, so a lane's traffic occupies exactly one contiguous stretch and three of four lanes are bare copper at any depth — not a defect, because those boundaries ARE the node's published tier floors expressed as a queue depth, drawn as vias. **Motion is honest and a PCB invites the dishonest kind**: every packet is STILL between polls, and the only moving thing is a dash-offset current carrying no reading, said on the view's face. **The clock is `useMemCanvas`'s elapsed `t`, NOT `Date.now()`** — the one place this view inverts orbital's/abyss's/pulse's choice, and principled: those three derive a COORDINATE from wall-clock, and nothing here is derived from time at all. **The reduced-motion twin therefore loses NOTHING**, true of no other canvas view. Fluid fourth time, first try: all six `naturalW == canvasW` cells EQUAL (1440 → 1180/1180, 1280 → 1020/1020, 390 → 366/366, BOTH feed states), no `.mp-fit`, authored 11px at 11px including at 390; height `clamp(w × 0.48, 250, 420)` → 353/302/250, c8 cell 596 against `canvasH` 702. Empty feed is an INTACT BOARD — lanes, rails, pads and ladder drawn, no die (no ceiling published), em-dashes throughout. **NINE DEFECTS FOUND BY LOOKING, none gate-visible**: 146 txs in the dense lane rendered as ONE SOLID BAR (exact tiling means adjacent packets share an edge — fixed with a hairline that is **VISUAL, NEVER POSITIONAL**, subtracted from the drawn WIDTH with the rect anchored at `cirX(ahead)`, so the tiling stays exact in DATA space; measured after: drawn rightmost edge **721.328125 == g.right EXACTLY**, leftmost 0.45px out, which is the floor's own bound); an invisible die; a cut that did not read IN THE TRAFFIC (now two alphas keyed on `fits`, bound `CIR_LANES` → `2 × CIR_LANES`, still constant); **`CIR_ASPECT`/`CIR_H_MAX` INERT** — `useChartMetrics`'s dead `k` in miniature, height was 300 at every real width; every depth label sitting on the die's warm substrate in the `dieFull` state; "chip" clipped structurally at the right edge; **the 390 axis reduced to TWO labels** by `fmtBytes`'s width; the tracked marker covering the lane above at 390; and two panel headers wrapping to three lines. **Plus one from reasoning** (`cirVias` walked ADJACENT tiers, so an empty MIDDLE lane would draw a route jumping two lanes with no connector — invisible on the fixture because ITS empty lane is at the END, where an adjacent walk is right for the wrong reason) **and one from measurement** (`CIR_PKT_MIN_W`'s docblock claimed the floor "does not bind at all" from a 1,010px board — that was the c8 CELL, not the 655.3px board; measured off the reduced-motion twin's DOM, **164 of 240 packets sit at the floor**, 60 drawn wider than true, worst overlap **0.476px**). **THREE BUDGET RAISES, NOT TWO, and the third is the brief's blind spot**: `verify-bundle`'s CHUNK COUNT is a centred ±4 drift detector that stood at 64 — the top of its band — and every net-new view adds EXACTLY one lazy chunk, so a chunk count is a VIEW-COUNT DERIVATIVE and this literal had been one view from firing since #177; red at 65 against [56,64], re-centred 60 → **61** per the file's own rule (widening loses sensitivity, moving keeps it), green against [57,65]. `lazyJsRaw` 790,000 → **820,000** (817,281, margin 2,719) and `totalJsRaw` 1,052,000 → **1,082,000** (1,079,307, margin 2,693), red-then-green on the FINAL tree — and the re-measurement rule earned its keep again, the first reading being +29,460 against a final +29,468, 8 B of drift that would have sat inside the margin unseen. Delta paired BY MULTIPLICITY, the `index` stem's two chunks moving by different amounts for the SECOND consecutive release (circuit +29,241 lazy · index/mapDeps +146 lazy · index entry +81 eager); 3 chunks moved of 65, the other 62 byte-identical, each budget reconciling exactly. **Baseline built in an ISOLATED `git worktree`** with its own dist/ and node_modules rather than by stashing in place — `git stash -u` is correct and is also one command from the shared-dist race, because a clean `git status` is not a clean SUBJECT. **The projection collapses here**: ten-view mean recounted to **25,404** (span 19,163–35,969), and only Relay remains, unprojectable and parked. Registration fills the three per-view maps (`DENSITY_FLOOR` 135 → **118** · `EXPECT_SVG_TEXT` `[]` verified 0 at four widths · `EXPECT_MEMSTAT` five keys, one of which — `bytes` — is the AXIS, taken as a PARAMETER, the first of the ten to anchor on that figure) and one `ROUTES` entry (46 → **47**); `FITS_AT_1440` correctly needs NO entry, and for a stronger reason than "fluid" — `verify-fit`'s `VIEWS` is a hardcoded fit-only list, confirmed at source. **The four-layer sweep is FOUR and a naive one finds five** (`verify-roles.mjs:204`'s `classic:` is the THEME id). Registration otherwise free: `verify-tracking` **80 → 89**, `verify-memstats` **39 → 40**, `verify-memviews` **276 ✅ · 11 ⚠️ · 0 ❌** — and the gate prints NO numeric tally, so every total ever quoted for it is an EXTRACTION; the 289 first reported here came from an `awk '…,0'` range that ran to end-of-file and swept the 29 gates after it, which is the subject-wider-than-the-claim family committed in a reported number; **`verify-nav` stayed at 129 and should have**. Standing rider taken: `pulse` promoted out of `verify-memshell`'s `NEW`, its 675 band now OWNED (the `(other stream)` marker gone), `NEW = ["relay","circuit"]`. Three-mutation break test, each red LOCALISED (M2 reds only at the mutated width, M3 only on circuit while nine views stay green), trap-owned restore verified clean. **`verify-vitals` red is PRE-EXISTING, measured against the baseline BUILD rather than a precedent**: `/` blocking **435 branch / 424 base**, both over 400, same route and assertion; markets and sim SKIPPED as UNVERIFIABLE on both by the gate's own contention guard; **`/live/mempool`, the only route Circuit touches, GREEN on both and better on the branch** (253/272). `verify:mem:perf` p5 **17 fps** against the 30 bar — FAIL, reported not hidden, bar untouched, no ranking claim. **FIVE HARNESS-LIES INSTANCES IN ONE RUN, all closed STRUCTURALLY, two of them new mechanisms**: a FILENAME that claimed a state its content never carried (closed with a `waitForSelector` before the shutter); a **silent no-op `str.replace` that printed `ok`** and rendered a "degraded" board that was fully live (applied ≠ effective, tooling edition — closed with an assert on the replacement count); a cwd reset that crashed a gate so the grep came back EMPTY, reading exactly like "no failures" (caught by checking the summary line, not the exit code); break-test extraction greps narrower than their claim with `head -N` truncating the evidence, so two mutations ran and their reds were never displayed (re-run with exact assertion text and no truncation — a break test whose output you cannot read is not a break test); and **a trap whose RELATIVE restore path landed in the wrong directory** after the script `cd`'d, leaving a stray copy while the explicit status check went green. Gates **77 files / 73 gates RECOUNTED and unchanged** — this PR adds none. N/M pair: **N 135** (floor 118) · **M 10**, the highest of the four net-new views. **No human has seen the rendered result in a browser.** https://github.com/aqua-019/satoshis-vision-v1/pull/178
- XMRIRISH-20260814-32 · done · p3·12 MARKETS canvas candles + brush — the hero is canvas (≤14 draw calls, bounded by construction, no text on the canvas), a brush strip windows the whole fetched span, and the D0847 accessible table ships with them. **The disease was measured before it was fixed and it was not where the brief said**: 30D was already 180 four-hour bars; 90D was 22, because `/ohlc` switches to FOUR-DAY buckets past 30 days. Drawn per range 42/180/22/91 → 43/180/**361**/122, with 7D and 30D deliberately unchanged (already at the upstream's finest TRUE granularity — moving them would trade real wicks for sampled ones). History is fetched as three BASES not per range: 30D cold 6→5 requests, all four ranges 21→10, `/ohlc` 4→1, and 7D/90D issue ZERO CoinGecko requests. `btcLine` was fetched every range change and rendered by nothing — deleted, and its absence is now the assertion. **Five defects found by LOOKING after the chain was green**, incl. a 90D axis reading "May '26 May '26 May '26" (format keyed on span, not tick step) and a price tick escaping the panel (no viewBox to clip a DOM label layer). **`cssColor`'s first home cost 757 EAGER bytes** — chart-kit is one hop from the entry through primitives.tsx — re-homed in a lazy-only leaf, verified in the built artifact. Five ceilings raised, each attributed to the byte; CHUNK_COUNT 61→62 re-centred (the 66th chunk is a shared LEAF, which that comment predicted would be Relay's). Two-polarity: 38 assertions red against an isolated-worktree `84e2b77` build, 0 on the branch. §5's seven-line pass descoped out loud. PR: (draft)
- XMRIRISH-20260814-32b · done · p3·12b one correction commit — **a wrong dollar figure on a live surface**: `attachVolume` closed its last bucket at `Infinity`, so a brushed window's final candle swept every sample to its right — **$9.7B in one 4h bucket** against $480M for every true one, in the table, the hover tip and a full-height VOL bar. No gate could see it: twenty-odd assertions on that page count bars, rows, labels, requests and pixels and none read a NUMBER it prints. **The first fix was wrong the other way** — also clipping volume to the drawn window under-reported the last cell by up to 4×, because `aggregateCandles` clips at CANDLE granularity while the clip clipped at SAMPLE granularity; caught by an adversarial pass before it shipped. **A third, reachable and never rendered**: `mid ?? deep` can hand DAILY samples to 4h buckets, so a coarser-than-bucket source is now refused outright (em-dash, never a synthesised split). **The break test went GREEN first time and that was the finding** — `win.to` had landed in the one bucket-phase in four where the clip is a no-op, so the max+median assertion was unproven; replaced with equality on EVERY cell, which reds at `[480M,480M,480M,480M,120M]`. Axis: at 7D the ~21h step emitted bare hours across seven dates, two of them IDENTICAL (9 ticks, 8 distinct) — a sub-daily label now carries its day. **Eleven stale self-counts corrected**, six of them p3·12's own and five pre-existing, two of those printed on every run (CSS source 203,896 → 254,399; `/mempool`'s "six view engines" → ten). **"gzip -9" names three compressors that disagree** (node:zlib 35,016 · CLI 34,969 · python 34,924) — every CLI-measured gzip figure was a true measurement of a quantity the gate never computes; re-measured with node:zlib over the real closure the route attributes to **residual ZERO** (+5,222). Three measurements of the eager delta, all correct, all different, because the tree moved between them. Gates 77/73 unchanged; all ceilings held. PR: #179
- XMRIRISH-20260815-33 · done · p3·12d VITALS CALIBRATION — the instrument repair #179's red demanded. No `src/**` file touched, build byte-identical; every defect is in the measuring apparatus. **A CEILING NEVER SATISFIABLE, AND TWO GREEN RUNS THAT CARRIED THE PROOF**: `/live/mempool`'s 4,000 ms LCP ceiling has never been met on CI's runner class — nine samples across runs #159/#160/#161, **three runners and TWO DIFFERENT TREES**, spanning **4076–4132, a plateau 1.4% wide**, against 3,684–3,732 locally. A plateau that tight across two trees and two runner states is a property of the RUNNER CLASS and of neither. **The brief named the wrong guard and so did p3·12b's own entry** — the spread guard has NEVER fired on that route (1.4% against a 10% threshold); both abstentions were the **CPU-probe** guard at 503/512 ms against a 260 ms reference, and one number disproves the attribution. Ceiling **DERIVED, not adopted**: high-water 4,132 × 1.05 — half of `LCP_SPREAD_UNSTABLE`'s own 10% band, so the headroom is the gate's own constant — = 4,338.6 → **4,350**; the full 10% (4,545) rejected in-file because the plateau is 1.4% wide and a 10% ceiling would need a 7×-the-noise regression before it spoke. **A calibration to the judging runner, not a concession.** Headroom now quoted one way throughout (as a % of the MEASURED value) after the first draft mixed conventions inside one comment block. **AN ABSTENTION THAT READ AS A PASS**: a decline printed its runs and threw the CONCLUSION away — #159 declined at **4080** and #160 at **4108** against a 4,000 ceiling, both reporting green, because the gate exits 0 when every route declines. Both paths now route through one `decline()` printing median · ceiling · would-have verdict · spread, shouting `VITALS_DECLINED_OVER_CEILING` when a declined median exceeds its ceiling. **The decision is WARN, NEVER FAIL, taken out loud** — failing on a number the gate just called UNVERIFIABLE is this file's own header inverted. **Three things make the warning countable anyway**: one skip per UNMADE ASSERTION rather than one per route (four declines said "4 skipped" for EIGHT assertions never made), the banner re-printed BELOW the tally where the eye stops, and an offline falsifiability PAIR over CI #161's real 4104 — fires against the retired 4,000, silent against the shipped 4,350, reading the SHIPPED literal so it reds if anyone lowers it. The shared reporter's four-counter contract (8 importers) deliberately NOT grown. **Two assertions were wider than what they could not judge**: the `continue` also discarded the structural "did the click land" check (three assertions silently never made in #159/#160), and the global interaction budget asserted ✅ over exactly the routes the same run had called unverifiable. **`main` had never been judged — not once**; `push: branches: [main]` added with the cost stated (the deterministic half IS duplicate on merge; accepted because the wall-clock half cannot be, and because `pull_request` CI never judged a MERGED tree either). Verified event-agnostic rather than assumed, and the comment block swept for `verify-*.mjs` FILENAMES because coldboot-live decides wiredness by substring over **raw ci.yml with YAML comments unstripped** — the first sweep used `verify-[a-z-]+\.mjs` and would have missed `verify-v508.mjs`. **THE RESTORE PROTOCOL FAILED TWICE**: a `cd` that did not survive made `git checkout` fail while the trailing grep printed EMPTY, reading exactly like a clean restore under a wrapper exit 0; then that leftover mutation was **committed by `git commit --amend`**, because the `git status` I checked was true and answered a narrower question than the one I drew from it. Caught by the mutator's own `assert n == 1` finding 0 matches for a string it had just replaced. Rules: **commit before EVERY break-test round; verify the COMMITTED BLOB, not the working tree; bracket every proves-an-absence grep so empty is distinguishable from crashed.** Gates **77 files / 73 gates RECOUNTED and unchanged**; CI unchanged at 30 `run:` lines / 21 gate-invoking / 62 distinct. Found NOT fixed: CLAUDE.md:73 says FOUR gates are in both the named list and `verify:static`; it is FIVE, and 62 reconciles only with five. Final tree: **verify-vitals 17 passed · 0 fixtured · 2 skipped · 0 failed**; `verify-bundle` 27/0 with every literal untouched; build inputs provably unchanged. Four break tests, each restore proven by tree state AND marker grep — **M4 is the sharpest**: putting the retired 4,000 back reds the offline self-check **before a browser launches** while the local judged assertion `3676ms ≤ 4000ms` PASSES. PR: #180
- XMRIRISH-20260815-34 · done · p3·13 MARKETS synced time cursor + the annotation layer — the four time-axis charts on `/live/markets` become one instrument, and the timeline the site already owned becomes something the price chart can point at. **THE SHARED VALUE IS A TIMESTAMP, NEVER A PIXEL**: four charts, four geometries and — the part that matters — four DOMAINS (the hero shows the brushed window, the ratio chart follows it, the two `MultiLine` groups show the RANGE), so a pixel is meaningless across the boundary and a fraction is worse, looking portable while silently meaning a different moment in every chart. `containsT(t, from, to)` is the one predicate and **a chart whose domain excludes `t` draws NOTHING — never a clamp**, the honest-motion doctrine applied to a cursor; because the write side still goes through chart-kit's `useSvgCursor`/`canvasCursor`, `verify-chartkit`'s "cursor math lives only in chart-kit" stays satisfied BY CONSTRUCTION rather than by an exemption. **Re-render ledger stated precisely because the naive version is four times worse**: subscribers are called from ONE rAF and mutate canvas/DOM through refs, so the chart under the pointer re-renders exactly as often as at `04006ff` and the other three re-render ZERO times — and the hero is strictly BETTER than before, its `cursorT` having been `useState` and now a ref, so a pointermove no longer rebuilds its label layer, its brush and the D0847 table's memo chain. Net renders added: none. **TWO NEW LAZY LEAVES WHOSE CHUNK FATES DIFFER FOR A REASON NOTHING IN THE SOURCE PREDICTS**: `data/timeline.ts` got its own 12,973 B chunk, `design/timeCursor.ts` was INLINED into `charts-*.js` — the difference is chunk GROUPS, not importer count, so the standing rule is not "a shared leaf costs a chunk" but **"a leaf shared ACROSS GROUPS costs a chunk"**, and only a build tells you which you wrote. **`eagerJsRaw` moved 23 B and every one is accounted for**: +30 B is one new string in Vite's `__vite__mapDeps` preload table and −7 B the rest of the entry shortening as the indices into it shift, measured (37 → 38 entries, that one addition, no removals, block +30, entry +23 — an earlier draft said +36/+6, true of a tree three commits old), with the negative control — `grep 'Bitcoin Whitepaper Published'` returning 0 in the entry AND vendor — as the half that matters. **AND THE LAZY-LEAF RULE'S OWN CLAIM WAS WRONG, MINE AND CLAUDE.md's**: both said the invariant "has no gate" and that `eagerJsRaw`'s headroom "would swallow a violation silently", asserted from a plausible mechanism. Measured by importing the leaf from the eager `App.tsx`: entry 99,973 → **112,914**, and `eagerJsRaw` 275,829 ≤ 280,000 PASSES, `eagerJsGz` PASSES, `lazyJsRaw` goes DOWN, `totalJsRaw` PASSES, chunk count falls 67 → 66 back into band as the leaf's chunk is ABSORBED — **every budget named for eager weight passes and the two detectors most likely to notice both move the reassuring way**. What reds is the PER-ROUTE table, as a side effect: eleven routes paying for something they never render. **The three that do not notice are `/`, `/live/markets` and `/learn`, and `/` is the LCP route the rule exists to protect.** It is a SIZE threshold, not a rule — the tightest route margin is 642 B gzip, so an eager leak under ~650 B gzip clears every ceiling in the file; 13 KB was loud, a helper would be silent. **THE TIMELINE'S DATES ARE POSITION-ONLY**: `d` stays the ONLY string any surface prints as a date, `iso`/`iso2`/`tent` place a flag and never become text, a day-precise event draws a point and anything wider draws a BAND over the interval it denotes with the dot at the interval's START rather than a midpoint nobody wrote down; intervals wider than 60% of the window carry no position and are counted ("undated at this zoom") instead of smeared. **The default view is EMPTY and that is the true answer**: `DEEP_DAYS` is 365 and the timeline starts in 2008, so at 30D the note reads "4 undated at this zoom · 45 outside this window" while the BRUSH STRIP — spanning the fetched span, not the window — still carries one flag, which is the entire argument for putting flags there. At 1Y: 4 in view, 3 groups, one a cluster of 2. **Extending `DEEP_DAYS` was NOT taken** — a request-budget and cache decision the §1d gate would own. **FOUR THINGS THE GATE CAUGHT IN ITS OWN FIRST DRAFT, three of them the standing family**: (1) a VACUOUS domain-mismatch proof — hovering a group chart at 2% of its width watched the hero draw nothing and passed, but 2% is inside the y-axis GUTTER where `MultiLine`'s own cursorT is null, so nothing was published and the hero had nothing to refuse; every mismatch assertion is now PAIRED with "the group chart is showing its own readout at this instant", and under M3 that guard reds at 0/12 rather than certifying; (2) a probe that HOVERED NOTHING — the group panels sit at y≈1358 below an 900px fold and `mouse.move` to an off-viewport point hits nothing, producing a convincing false defect ("the group charts do not sync"), caught by `elementFromPoint` returning `none` and now an assertion rather than a comment; (3) **`\b17\b` does not match `Jul 17XMR`** — the readout runs the date into the first series name, so a word-boundary check went red against four charts that agreed perfectly; (4) an assertion that was simply the WRONG STATEMENT — "only imprecise dates draw a band" reds against correct code, because a day-precise event denotes a whole DAY, ~3px at a one-year zoom, and the band is driven by rendered EXTENT not by a precision category; replaced with the assertion that has content, a month reading **81.9px against a day's 2.6px at the same zoom**. **`verify-hero` went red on the extraction and was right**: `passages.ts`'s `monero-launch` record cites the file its quotation lives in, the quotation moved, and both halves failed — a citation is a claim about WHERE a sentence is, and that claim had become false. **`verify-govern` went red on both new rAF call sites**, and the fix taught something: the cursor's marker belongs on the `raf` helper's DECLARATION, not beside the `raf(...)` call, because the gate sweeps for `requestAnimationFrame` and the indirection is the only place that word appears — a marker beside the call is an ORPHAN, which the gate reports separately and correctly. Budgets, red-then-green on the FINAL tree, paired BY MULTIPLICITY (the `index` stem holds two chunks; the pairing asks `dist/index.html` which is the entry, because a basename diff would file a LAZY delta under the EAGER budget): `cssGz` 17,600 → **18,200** · `lazyJsRaw` 831,000 → **845,000** · `totalJsRaw` 1,094,000 → **1,108,000** · `/live/markets` 112,000 → **121,000** · `CHUNK_COUNT` 62 → **64** (±4 unchanged; 64 rather than 63 so reality is not sitting on the ceiling — one rung of headroom, said in the file). FIVE chunks moved of 67, the other 62 byte-identical (timeline +12,973 · EducationPage −9,218 · MarketsPage +5,899 · charts +3,931 · entry +23); lazy +13,585 and total +13,608 reconcile to the byte. Gates **77 files / 73 gates RECOUNTED and unchanged** (77 = 73 + 3 shared modules + the orchestrator); CI unchanged at 30 `run:` lines / 21 gate-invoking / **62 distinct** (68 invocations − 6 duplicates). `verify-markets-dom` **74 → 118 assertions** (the gate prints no tally, so quote the instrument: `grep -c '✅'` bounded to its own range reads 75 → 119, one line of each being the summary). **`verify-effects`' ledger did NOT move and should not have** — its completeness sweep is scoped to `src/data/` only, so this PR's three new effects are outside it by design, worth knowing before assuming that gate covers a hook you just wrote. Six break tests, each restore proven by `git status`, by `diff` against the COMMITTED BLOB, and by a bracketed marker sweep. **THREE DEFECTS FOUND BY LOOKING AT THE RENDER**, the first created by the SYNC out of two correct pieces: `fmtDate` renders "Feb 26" for FEBRUARY 2026 — fine on an axis where neighbours disambiguate, and a lie next to the hero's "2026-02-12" when two charts report ONE moment, so the readout emits an ISO day (the axis untouched, since changing `fmtDate` moves every tick in the app); the layer toggles sat BELOW THE FOLD because the page mounted them after `</CandleCanvas>` while the D0847 table lives inside it, so a control for marks at the top of the plot was reachable only by scrolling past a 210px table it does not govern; and a 9px flag was the whole touch target — the 26px cluster rule turns out to be load-bearing twice, since WCAG 2.2 AA 2.5.8's SPACING exception was already satisfied by it, and a `::after` grows the hit area to 23px which still fits inside that spacing. **The gate's own "one moment" assertion was PHASE-DEPENDENT** — it asserted the hero's string EQUALS the synced one, true only when the cursor lands on a bucket boundary; at 390 the hero read `2026-02-18` (the 3-day candle it hovered) against `2026-02-20` (the moment), both correct. Replaced with two claims: the three synced readouts are byte-identical, and the hero's BUCKET contains that moment. **The render probe's `shot()` was wrong three times, each the same species** — file written, name confident, content of something else: `elementHandle.screenshot()` scrolls and a scroll under a stationary pointer fires pointerleave; `page.screenshot({clip})` rejects an out-of-viewport clip; `{fullPage:true,clip}` mis-places it because `boundingBox()` is viewport-relative while a fullPage clip is in page coordinates. The shutter now refuses to fire unless what the filename claims is measurably on screen. **`git add -A` swept a scratch probe into a commit** (`app/.render-p313.mjs`), found only because the stray-file sweep ran against `git ls-tree` rather than the working tree — a working-tree check says nothing about what is already committed. Pre-existing and NOT fixed, proven structurally: the hero's date axis collides at 390 on 1Y, and `candle-data.ts`/`useChartMetrics.ts` are byte-identical to `04006ff`, so the axis is a pure function of untouched inputs. **No human has seen the rendered result in a browser.** PR: https://github.com/aqua-019/satoshis-vision-v1/pull/181
